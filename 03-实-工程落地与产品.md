# 第三幕 · 实 —— 工程落地：产品、预警与能力进阶

> **核心目标**：将八大机制落回真实世界。梳理 10+ Harness 产品的归类，理解 Anthropic 对 Agent 的三个预警，
> 通过三层次能力对比理解 Harness 带来的质的飞跃，最终建立「Agent = Model + Harness」的完整认知。

---

## 目录

- [一、Agent = Model + Harness：公式的完整展开](#一agent--model--harness公式的完整展开)
  - [1.1 公式图解](#11-公式图解)
  - [1.2 Model：引擎的边界](#12-model引擎的边界)
  - [1.3 Harness：底盘的构成](#13-harness底盘的构成)
  - [1.4 上限与下限的辩证关系](#14-上限与下限的辩证关系)
- [二、10+ Harness 产品归类](#二十-harness-产品归类)
  - [2.1 产品全景图](#21-产品全景图)
  - [2.2 第一类：通用 Agent 平台](#22-第一类通用-agent-平台)
  - [2.3 第二类：编码 Agent](#23-第二类编码-agent)
  - [2.4 第三类：框架与运行时](#24-第三类框架与运行时)
  - [2.5 第四类：专项 Agent](#25-第四类专项-agent)
  - [2.6 各产品 Harness 能力对比](#26-各产品-harness-能力对比)
- [三、Anthropic 的三个预警](#三anthropic-的三个预警)
  - [3.1 预警一：Agent 不是魔法](#31-预警一agent-不是魔法)
  - [3.2 预警二：简单问题不需要 Agent](#32-预警二简单问题不需要-agent)
  - [3.3 预警三：不可逆操作必须人工确认](#33-预警三不可逆操作必须人工确认)
- [四、三层次能力对比：从裸模型到 Claude Code](#四三层次能力对比从裸模型到-claude-code)
  - [4.1 Level 1：裸模型 LLM](#41-level-1裸模型-llm)
  - [4.2 Level 2：LLM + Code Interpreter](#42-level-2llm--code-interpreter)
  - [4.3 Level 3：LLM + Harness (Claude Code)](#43-level-3llm--harness-claude-code)
  - [4.4 差距到底在哪？](#44-差距到底在哪)
- [五、Harness 的深层价值：让它跑起来 vs 让它跑下去](#五harness-的深层价值让它跑起来-vs-让它跑下去)
- [六、本章小结](#六本章小结)

---

## 一、Agent = Model + Harness：公式的完整展开

### 1.1 公式图解

```
┌─────────────────────────────────────────────────────────┐
│                        AGENT                            │
│                                                         │
│  ┌──────────────────────┐  ┌──────────────────────────┐ │
│  │       MODEL          │  │        HARNESS           │ │
│  │      (引擎)           │  │       (驾驭系统)          │ │
│  │                      │  │                          │ │
│  │  · GPT-4/5           │  │  · 四相循环 (方向盘)       │ │
│  │  · Claude 3.5/4      │  │  · 工具编排 (油门+仪表)    │ │
│  │  · Gemini 2.5        │  │  · 上下文管理 (挡风玻璃)    │ │
│  │  · DeepSeek          │  │  · 进度跟踪 (行车记录仪)    │ │
│  │  · Qwen              │  │  · 权限闸 (刹车)          │ │
│  │                      │  │  · 验证循环 (导航校验)     │ │
│  │  决定: 上限           │  │  · 子代理分治 (车队调度)    │ │
│  │  (最好能有多好)        │  │  · GAN评审 (质检站)       │ │
│  │                      │  │  · Token预算 (油耗表)      │ │
│  └──────────────────────┘  └──────────────────────────┘ │
│                                                         │
│  上限靠模型，下限靠工程。                                   │
│  引擎好 + 底盘稳 = 真正能跑长途的 Agent                       │
└─────────────────────────────────────────────────────────┘
```

### 1.2 Model：引擎的边界

**Model（大语言模型）本质上能做三件事**：

1. **理解**：解析自然语言，理解意图和上下文
2. **推理**：基于已有知识进行逻辑推理、代码生成、文本生成
3. **决策**：在给定信息下选择调用哪个工具、给出什么答案

**Model 本质上做不到的事**：

| Model 做不到的 | 为什么做不到 |
|---------------|------------|
| 记住 30 分钟前的对话 | Context 窗口有限，没有持久化机制 |
| 知道自己不知道什么 | LLM 没有"未知"的概念，只会尽力猜测（幻觉） |
| 在进程 crash 后恢复 | 它是无状态函数，没有记忆 |
| 拒绝执行危险操作 | 它不理解"危险"的真实含义 |
| 管控自己的运行成本 | 它不知道每次调用花了多少钱 |
| 验证自己产出的正确性 | 它对自己的输出没有独立评判能力 |

**Model 越强，越需要 Harness**：一个强大的模型在失控时破坏力也更大——就像一个马力更大的引擎在没有刹车的车上更危险。

### 1.3 Harness：底盘的构成

```
Harness (驾驭系统) = 底盘 + 刹车 + 方向盘 + 仪表盘 + 导航

┌──────────────┬──────────────────────────────────┐
│ 底盘         │ 四相循环 + 上下文工程                │
│ (运行基础)    │ → 让 Agent 有一个稳定的运行框架       │
├──────────────┼──────────────────────────────────┤
│ 刹车         │ 权限闸 + Token 预算                │
│ (安全边界)    │ → 危险时能停，超预算时能止            │
├──────────────┼──────────────────────────────────┤
│ 方向盘       │ 任务拆解 + 子代理分治               │
│ (方向控制)    │ → 确保 Agent 往正确的方向走           │
├──────────────┼──────────────────────────────────┤
│ 仪表盘       │ 工具编排 + 进度跟踪                 │
│ (可观测性)    │ → 实时知道 Agent 在做什么，做到哪了     │
├──────────────┼──────────────────────────────────┤
│ 导航校验     │ 验证循环 + GAN 三角色               │
│ (质量保证)    │ → 不是"觉得到了"，而是"验证到了"      │
└──────────────┴──────────────────────────────────┘
```

### 1.4 上限与下限的辩证关系

```
场景 A: 强 Model + 弱 Harness
  → 单次回答质量极高，但 30 分钟后崩溃
  → 适合：单轮问答（ChatGPT 网页版）
  → 不适合：任何需要多步骤操作的工程任务

场景 B: 弱 Model + 强 Harness
  → 运行稳定，但单次推理能力有限
  → 适合：自动化流水线（如 CI/CD 中的固定脚本）
  → 不适合：需要深度推理的创造性任务

场景 C: 强 Model + 强 Harness  ← 目标
  → 既能深度推理，又能持续稳定运行
  → 适合：复杂工程任务、长时 Agent 工作
  → 代表：Claude Code、Devin、Cursor Agent

关键认知：
  你的 Agent 能做到多好，取决于 Model；
  你的 Agent 能稳定地做到多好，取决于 Harness。
  两者缺一不可。
```

---

## 二、10+ Harness 产品归类

### 2.1 产品全景图

```
                         Harness 产品全景图 (2024-2025)
┌──────────────────────────────────────────────────────────────────┐
│                                                                   │
│  通用 Agent 平台                                                   │
│  ┌──────────┐ ┌──────────┐ ┌──────────┐ ┌──────────────────┐    │
│  │ ChatGPT  │ │  Claude  │ │  Gemini  │ │ 开源 (Open WebUI  │    │
│  │  +Tools  │ │  +MCP    │ │  +Tools  │ │  + Function Call) │    │
│  └──────────┘ └──────────┘ └──────────┘ └──────────────────┘    │
│                                                                   │
│  编码 Agent                                                       │
│  ┌──────────┐ ┌──────────┐ ┌──────────┐ ┌──────────────────┐    │
│  │  Claude  │ │  Cursor  │ │  Devin   │ │  GitHub          │    │
│  │  Code    │ │  Agent   │ │  (Cognition)│  Copilot        │    │
│  └──────────┘ └──────────┘ └──────────┘ └──────────────────┘    │
│                                                                   │
│  框架与运行时                                                      │
│  ┌──────────┐ ┌──────────┐ ┌──────────┐ ┌──────────────────┐    │
│  │LangChain │ │LangGraph │ │DeepAgents│ │  AutoGen         │    │
│  │(框架)     │ │(运行时)   │ │(驾驭)     │ │  (多Agent)       │    │
│  └──────────┘ └──────────┘ └──────────┘ └──────────────────┘    │
│                                                                   │
│  专项 Agent                                                       │
│  ┌──────────┐ ┌──────────┐ ┌──────────┐ ┌──────────────────┐    │
│  │ Browser  │ │  Data    │ │  Customer│ │  Code Review     │    │
│  │ Use      │ │  Analyst │ │  Support │ │  Agent           │    │
│  └──────────┘ └──────────┘ └──────────┘ └──────────────────┘    │
│                                                                   │
└──────────────────────────────────────────────────────────────────┘
```

### 2.2 第一类：通用 Agent 平台

这些产品给模型加上基础工具调用，但不做深度 Harness 工程化。

| 产品 | 核心模式 | Harness 水平 | 适用场景 |
|------|---------|------------|---------|
| **ChatGPT + Tools/Plugins** | LLM + Function Calling | ⭐⭐ (基础工具调用，有 code interpreter) | 数据分析、文档处理 |
| **Claude + MCP** | LLM + MCP 协议工具 | ⭐⭐⭐ (标准化工具接入 + Projects 知识) | 知识工作、编程辅助 |
| **Gemini + Tools** | LLM + Google 生态 | ⭐⭐ (Google Search/Workspace 集成) | 信息检索、文档协作 |
| **开源方案** (Open WebUI + Pipes) | LLM + 自定义 Function | ⭐ (需要自己搭建 Harness) | 自托管、隐私敏感场景 |

**共同特点**：
- 工具调用是主要（有时是唯一）的 Harness 机制
- 没有任务拆解、没有断点续传、没有子代理
- 适合 **短任务**（< 10 分钟），不适合长时自主运行

### 2.3 第二类：编码 Agent

编码是 Harness Engineering 最成熟的落地领域。代码的可验证性（编译、测试、lint）让 Harness 机制能充分发挥。

| 产品 | 核心 Harness 机制 | 亮点 | 不足 |
|------|------------------|------|------|
| **Claude Code** | 四相循环、任务拆解、子代理、权限闸、进度跟踪、GAN 评审、Token 预算、27 种 Hook | Harness 机制最全面的产品 | CLI 形态，有一定学习曲线 |
| **Cursor Agent** | 代码索引 (Context)、工具调用、内联编辑 | 交互体验好，Context 工程优秀 | Agent 长时运行稳定性待提升 |
| **Devin (Cognition)** | 独立 VM 环境、任务拆解、进度追踪 | 完全隔离的沙箱，可长时间自主工作 | 速度较慢，费用高 |
| **GitHub Copilot** | 代码补全 + Agent Mode + 工作区索引 | 无缝 IDE 集成，Context 感知好 | Agent 模式起步较晚，Harness 还在完善 |
| **Aider** | Git 集成、Map-Refine 编辑、测试驱动 | 开源，Git diff 原生，轻量 | Harness 机制相对简单 |

**编码 Agent 的特殊优势**：
- **天然验证手段**：代码可以编译、测试、lint，让验证循环能充分发挥
- **Git 天然进度追踪**：每次成功的修改就是一个 commit，天然 checkpoint
- **文件系统是天然状态**：文件内容 = Agent 的持久化产出，天然抗 crash

### 2.4 第三类：框架与运行时

这些不是直接面向用户的 Agent 产品，而是构建 Agent 的工程基础设施。

| 框架 | 定位 | 核心贡献 | Harness 成熟度 |
|------|------|---------|--------------|
| **LangChain** | Agent 框架 | 标准化了 Chain/Agent/Tool 的抽象 | ⭐⭐ (工具调用的框架化) |
| **LlamaIndex** | 数据 Agent 框架 | 标准化了 RAG/索引/查询的抽象 | ⭐⭐ (数据检索的标准化) |
| **LangGraph** | Agent 运行时 | 图结构的状态管理和流程编排 | ⭐⭐⭐ (流程可控、可分支、可循环) |
| **DeepAgents** | 驾驭级 Agent 框架 | 任务拆解、进度持久化、子代理隔离、文件系统工具 | ⭐⭐⭐⭐ (最接近完整 Harness) |
| **AutoGen (Microsoft)** | 多 Agent 对话框架 | 多 Agent 对话模式和角色分配 | ⭐⭐⭐ (多 Agent 协作的标准化) |
| **CrewAI** | 多 Agent 编排 | 角色定义、任务分配、顺序/层级流程 | ⭐⭐⭐ (多 Agent 协作) |
| **Agno** | 轻量 Agent 框架 | 极简 API，多模态，快速开发 | ⭐⭐ (轻量和速度优先) |

**LangChain → LangGraph → DeepAgents 的演进含义**：

```
LangChain (2022-2023)
  关键词: 框架 (Framework)
  问题: "如何用代码写出一个 LLM 调用链？"
  答案: Chain 抽象、Agent 抽象、Tool 抽象
  局限: 线性思维，对复杂分支流程控制力弱

        ↓ 从"框架"到"运行时"

LangGraph (2024)
  关键词: 运行时 (Runtime)
  问题: "如何让 Agent 流程可控、可分支、可循环、可恢复？"
  答案: 图结构 State Machine、Checkpoint、Human-in-the-Loop
  局限: 仍然需要开发者手写大量控制逻辑

        ↓ 从"运行时"到"驾驭"

DeepAgents (2025)
  关键词: 驾驭 (Harness)
  问题: "如何让 Agent 长期自主运行而不崩？"
  答案: 内置任务拆解、进度持久化、子代理隔离、文件系统工具
  特征: Harness 不再是开发者手写的，而是框架内置的
```

### 2.5 第四类：专项 Agent

针对特定领域的 Harness Engineering。

| 领域 | 代表产品 | 专属 Harness 需求 |
|------|---------|------------------|
| **浏览器自动化** | Playwright Agent, Browser-Use | DOM 感知、视觉验证、反检测 |
| **数据分析** | ChatGPT Code Interpreter, Julius | 沙箱安全、大数据量处理、结果可视化验证 |
| **代码审查** | CodeRabbit, Copilot Review | Diff 理解、风格规范、安全规则 |
| **客户支持** | Intercom Fin, Zendesk AI | 知识库集成、人工接管、敏感信息脱敏 |
| **DevOps** | Kubiya, PagerDuty AIOps | 基础设施权限安全、变更窗口管理、回滚机制 |

### 2.6 各产品 Harness 能力对比

```
                 四相   工具   进度   上下文  任务   验证   子代理  GAN   权限
                 循环   编排   跟踪   工程    拆解   循环   分治   三角色  闸
                 ────  ────  ────  ──────  ────  ────  ────  ──────  ────
Claude Code       ✅    ✅    ✅     ✅     ✅    ✅     ✅     ✅     ✅
Devin             ✅    ✅    ✅     ✅     ✅    ⚠️     ⚠️     ❌     ✅
Cursor Agent      ⚠️    ✅    ❌     ✅     ⚠️    ⚠️     ❌     ❌     ✅
GitHub Copilot    ❌    ✅    ❌     ✅     ❌    ⚠️     ❌     ❌     ⚠️
Aider             ⚠️    ✅    ❌     ❌     ❌    ✅     ❌     ❌     ⚠️
DeepAgents        ✅    ✅    ✅     ⚠️     ✅    ❌     ✅     ❌     ❌
LangGraph         ⚠️    ⚠️    ⚠️     ❌     ❌    ❌     ❌     ❌     ⚠️
ChatGPT+Tools     ❌    ⚠️    ❌     ❌     ❌    ❌     ❌     ❌     ❌

✅ = 内置
⚠️ = 部分/需要配置
❌ = 不支持
```

> **结论**：Claude Code 是当前 Harness 机制最全面的产品，这也是为什么本课程以 Claude Code 为主要参考对象。

---

## 三、Anthropic 的三个预警

Anthropic 在 2025 年发布了关于 Building Effective Agents 的指南，其中包含三个重要的预警信号。这些预警实际上是 Harness Engineering 的「负面清单」——告诉你什么时候**不应该**用 Agent。

### 3.1 预警一：Agent 不是魔法

> **原文精神**：Agent 是对 LLM 行为的系统性工程约束，不是"给 LLM 加上工具就能产生智能"的魔法。

**解读**：

很多人第一次接触 Agent 框架时的反应是：

> "哇，我只要给 LLM 装上 10 个工具，它就能自动帮我运营电商店铺了！"

这是**魔法思维**。Agent 的行为质量 = 模型能力 × 工具质量 × Harness 设计。三个因子中任何一个为 0，结果就是 0。

**意味着什么**：

| 魔法思维 | 工程思维 |
|---------|---------|
| "装上搜索引擎 Agent 就能做研究了" | 检索结果如何格式化？多轮检索如何避免重复？检索噪音如何过滤？ |
| "装上数据库工具 Agent 就能查数了" | SQL 注入怎么防？查询超时怎么处理？结果集太大怎么办？ |
| "给个 while 循环 Agent 就能自己工作了" | 循环什么时候停？怎么知道走对了方向？crash 了怎么恢复？ |

**对 Harness Engineer 的启示**：每个工具都代表一个工程问题，不是你定义好 schema 就完了。

### 3.2 预警二：简单问题不需要 Agent

> **原文精神**：如果一个任务可以用一次 LLM 调用 + 固定流程完成，就不需要 Agent。

**判断标准**：

```
是否需要用 Agent？问自己三个问题：

Q1: 任务需要超过 1 次 LLM 调用吗？
  否 → 不需要 Agent。用单次 API 调用 + 后处理即可。
  
Q2: 执行路径在运行前不能完全确定吗？
  否 (可以确定) → 不需要 Agent。用固定 workflow (LangGraph / 状态机) 即可。
  
Q3: 任务需要跨 session 的持久化状态吗？
  否 → 可能不需要完整的 Agent。用短时对话 + 工具调用即可。
```

**典型不需要 Agent 的场景**：
- 邮件自动回复分类器（固定规则 + LLM 分类）
- 文档摘要（单次 API 调用）
- 固定流程的数据 ETL（有向无环图，没有分支判断）
- 代码审查的固定检查项（linter + 静态分析）

**典型需要 Agent 的场景**：
- "修复这个 bug，我不确定它在哪个文件里"（需要多轮探索 + 验证）
- "把这个功能从 A 服务迁移到 B 服务"（需要多步操作 + 验证 + 可能回滚）
- "升级这个项目的依赖到最新版"（每个依赖可能遇到不同的问题需要逐一解决）

### 3.3 预警三：不可逆操作必须人工确认

> **原文精神**：任何会产生不可逆后果的操作，Agent 不能独立决策，必须有人的审批环节。

**什么是"不可逆操作"**：

```
高风险层 (必须人工确认):
  - git push --force
  - 数据库 DROP / DELETE / TRUNCATE
  - 删除文件（特别是未提交的代码）
  - 修改生产环境配置
  - 发送邮件/消息给真实用户
  - 调用付费 API（可能产生大量费用）
  - 修改系统级配置

中风险层 (可配置是否需要确认):
  - git push (非 force)
  - 修改依赖文件 (package.json, requirements.txt)
  - 执行网络请求 (WebFetch, curl)
  - 创建/修改超过 N 行的文件
  - 安装新的工具/依赖

低风险层 (可以自动执行):
  - 读取文件
  - 搜索代码 (grep/glob)
  - 运行只读命令 (git status, git log, ls)
  - 在隔离沙箱内执行代码
```

**设计原则**：**"先问再做的代价远低于做了再后悔的代价"**。

---

## 四、三层次能力对比：从裸模型到 Claude Code

这是整个课程最核心的对比。三层次对应同一个技术栈下的三种不同产品形态，直观展示了 Harness 带来的质变。

### 4.1 Level 1：裸模型 LLM

```
产品形态: API 调用、ChatGPT 网页版、Claude.ai 聊天界面
Harness 层: 几乎为零（只有基础的多轮对话管理）

工作流:
  用户输入 → LLM 推理 → 文本输出
  
能力边界:
  ✅ 单次文本生成/翻译/摘要 → 很强
  ✅ 代码片段生成 → 很强
  ✅ 回答知识性问题 → 很强（但有幻觉风险）
  ⚠️ 多轮对话 → 可用，但长对话质量下降（context 限制）
  ❌ 读文件 → 不能直接读，只能让用户粘贴
  ❌ 执行代码 → 不能
  ❌ 跨工具编排 → 不能
  ❌ 30分钟+持续工作 → 不能（对话 reset 或 context 爆）
  ❌ 断点续传 → 不能（关掉就没了）
  ❌ 真机验证 → 不能（只能"说"对错）
```

### 4.2 Level 2：LLM + Code Interpreter

```
产品形态: ChatGPT Code Interpreter, Claude Artifacts
Harness 层: 薄层 ⭐⭐ (沙箱化 Python 运行时 + 文件上传)

工作流:
  用户输入 → LLM 推理 → 调用 Python → 返回结果 → LLM 解读 → 输出

新增能力 (vs Level 1):
  ✅ 执行 Python → 可以验证代码逻辑
  ✅ 文件上传/下载 → 可以处理用户数据
  ✅ 图表生成 → matplotlib/plotly 可视化
  ✅ 基础多步推理 + 验证

仍然做不到 (vs Level 3):
  ❌ 读任意文件 → 只能读沙箱内的文件，不能访问项目文件系统
  ❌ 跨工具编排 → 只有 Python，不能同时用 Shell、浏览器、数据库
  ❌ 30分钟+持续工作 → context 管理仍然原始
  ❌ 断点续传 → 不支持
  ❌ 权限管控 → 沙箱内部无限制
  ❌ 自主任务拆解 → 需要用户一步步引导
```

### 4.3 Level 3：LLM + Harness (Claude Code)

```
产品形态: Claude Code, Cursor Agent, Devin
Harness 层: 厚层 ⭐⭐⭐⭐⭐ (八大机制 + 三支柱 + Hook 系统)

工作流:
  用户输入任务 → 
    [任务拆解] → 子任务清单
    [四相循环] → 对每个子任务:
      Gather → Act → Verify → Iterate
        ↓          ↓        ↓
    [子代理]   [工具编排]  [验证循环]
        ↓          ↓        ↓
    [上下文工程] [权限闸]   [GAN评审]
        ↓          ↓        ↓
    [进度跟踪] [Token预算]  [Hook触发]
    → 交付可验证的产出

能力全开:
  ✅ 读取任意文件/项目结构 → Bash(grep), Glob, Read 工具
  ✅ 跨工具编排 → 20+ 工具同时可用
  ✅ 30分钟+持续工作 → 上下文工程管理
  ✅ 断点续传 → 进度跟踪 + git checkpoint
  ✅ 权限管控 → 分层权限 + 人工审批
  ✅ 自动化评审 → 验证循环 + Evaluator
  ✅ Token 预算 → 成本可控
  ✅ 自主任务拆解 → 大任务自动拆小
```

### 4.4 差距到底在哪？

```
能力维度                          Level 1   Level 2   Level 3
                                 (裸LLM)   (+Code)   (+Harness)
──────────────────────────────────────────────────────────────
读取文件                           ❌        ⚠️        ✅
写文件(持久化)                      ❌        ❌        ✅
执行代码                           ❌        ✅        ✅
跨工具编排(多工具协同)               ❌        ❌        ✅
持续30分钟以上对话                   ❌        ❌        ✅
Task Decomposition (任务拆解)        ❌        ❌        ✅
断点续传 (Crash Recovery)           ❌        ❌        ✅
自主错误恢复                         ❌        ⚠️        ✅
权限分层管控                         ❌        ❌        ✅
自动化质量验证                        ❌        ⚠️        ✅
Token Budget 控制                    ❌        ❌        ✅
Subagent 分治                        ❌        ❌        ✅
Hook 系统                            ❌        ❌        ✅
Prompt Cache 优化                    ❌        ❌        ✅
──────────────────────────────────────────────────────────────
关键差异:
  Level 1 → Level 2: Model + 沙箱化的单一工具
  Level 2 → Level 3: Model + 完整的 Harness 系统
```

**核心洞察**：

从 Level 2 到 Level 3 的跳跃，不是模型的跳跃（你可以在 Level 2 和 Level 3 用同一个模型），而是 **Harness 的跳跃**。

这也是整个课程的论点：**Agent 不是 Model 的升级版，而是加上了 Harness 的完整系统。**

---

## 五、Harness 的深层价值：让它跑起来 vs 让它跑下去

```
让它跑起来 (Get it running):
  - 给 LLM 装上工具
  - 写一个 while 循环
  - 花 30 分钟实现一个 Demo
  - 发一条推特: "我用 100 行代码做了一个自主 Agent！"

让它跑下去 (Keep it running):
  - 设计错误处理、重试策略、降级方案
  - 管理 context 生命周期，防止溢出
  - 持久化状态，保证 crash 可恢复
  - 建立权限模型，防止越界操作
  - 设置 token 预算，控制成本
  - 引入验证机制，确保交付质量
  - 实现可观测性，知道 Agent 在做什么
  - 支持子任务隔离，并行和分治
  - 搭建多角色评审，防止自我欺骗
  - 花 6 个月构建一个生产可用的 Agent 系统
```

**Harness Engineering 的本质**：

> 把"能用"变成"可靠"；
> 把"有时候对"变成"每次都验证"；
> 把"跑一次"变成"持续运行"；
> 把"信任模型"变成"验证模型"。

---

## 六、本章小结

1. **Agent = Model + Harness** 不仅是公式，更是工程实践的指导原则。
   - 评估 Agent 时，先拆开看：Model 能力是多少分？Harness 设计是多少分？
   - 建设 Agent 时，两个维度都要投入。

2. **Harness 产品生态正在形成**，从通用平台的薄 Harness，到编码 Agent 的厚 Harness，再到框架的 Harness 基础设施化。

3. **Anthropic 的三个预警**为 Harness Engineer 划定了清晰的"不要做什么"的边界：
   - Agent 不是魔法，每个工具都是工程问题
   - 简单问题不需要 Agent
   - 不可逆操作必须人工确认

4. **三层次对比直接证明了 Harness 的价值**：同一个模型，加上不同层次的 Harness，展现的能力天差地别。

5. **从 Level 2 到 Level 3 的跳跃是 Harness 的跳跃，不是模型的跳跃。** 这才是 Harness Engineer 存在的根本理由。

---

> **继续阅读**：
> - [八大机制详解](./mechanisms/) —— 每个机制的深入分析
> - [三支柱详解](./pillars/) —— 输入流、控制流、状态流的深入分析
> - [对照矩阵](./matrices/) —— 完整的问题-机制-支柱对照
> - [Harness Engineer 能力模型](./harness-engineer/) —— 成为一名 Harness Engineer 需要掌握什么
