# 第一幕 · 破 —— 打破直觉，重新理解 Agent

> **核心目标**：打碎「Agent = LLM + 工具调用」的朴素直觉，揭示 Naive Agent 的八种致命失效形态，建立 Agent = Model + Harness 的认知框架。

---

## 目录

- [一、打破直觉：Agent ≠ LLM + 工具调用](#一打破直觉agent--llm--工具调用)
  - [1.1 你的直觉是什么？](#11-你的直觉是什么)
  - [1.2 第一个公式：Agent = Model + Harness](#12-第一个公式agent--model--harness)
  - [1.3 引擎与底盘：一个汽车的隐喻](#13-引擎与底盘一个汽车的隐喻)
- [二、Naive Agent：你以为的 Agent 长这样](#二naive-agent你以为的-agent-长这样)
  - [2.1 最小可行 Agent 代码](#21-最小可行-agent-代码)
  - [2.2 这段代码的朴素假设](#22-这段代码的朴素假设)
- [三、八种失效形态：逐一致命解剖](#三八种失效形态逐一致命解剖)
  - [3.1 失效 1：循环失控 (Loop Runaway)](#31-失效-1循环失控-loop-runaway)
  - [3.2 失效 2：Context 溢出 (Context Overflow)](#32-失效-2context-溢出-context-overflow)
  - [3.3 失效 3：Cache Miss (Prompt Cache 命中失败)](#33-失效-3cache-miss-prompt-cache-命中失败)
  - [3.4 失效 4：Tool 错误吞并 (Tool Error Swallowing)](#34-失效-4tool-错误吞并-tool-error-swallowing)
  - [3.5 失效 5：状态丢失 (State Loss)](#35-失效-5状态丢失-state-loss)
  - [3.6 失效 6：缺权限闸 (Missing Permission Gate)](#36-失效-6缺权限闸-missing-permission-gate)
  - [3.7 失效 7：缺自动化评审 (Missing Automated Review)](#37-失效-7缺自动化评审-missing-automated-review)
  - [3.8 失效 8：成本失控 (Cost Runaway)](#38-失效-8成本失控-cost-runaway)
- [四、失效时间线：从启动到崩溃](#四失效时间线从启动到崩溃)
- [五、同任务三模式演示：差距一目了然](#五同任务三模式演示差距一目了然)
  - [5.1 任务描述](#51-任务描述)
  - [5.2 裸模型 LLM 的表现](#52-裸模型-llm-的表现)
  - [5.3 LLM + Code Interpreter 的表现](#53-llm--code-interpreter-的表现)
  - [5.4 Claude Code (LLM + Harness) 的表现](#54-claude-code-llm--harness-的表现)
  - [5.5 三层次能力对比矩阵](#55-三层次能力对比矩阵)
- [六、三代工程：演进而非替代](#六三代工程演进而非替代)
  - [6.1 第一代：提示词工程 (Prompt Engineering)](#61-第一代提示词工程-prompt-engineering)
  - [6.2 第二代：上下文工程 (Context Engineering)](#62-第二代上下文工程-context-engineering)
  - [6.3 第三代：驾驭工程 (Harness Engineering)](#63-第三代驾驭工程-harness-engineering)
  - [6.4 框架演进：LangChain → LangGraph → DeepAgents](#64-框架演进langchain--langgraph--deepagents)
- [七、本章小结](#七本章小结)

---

## 一、打破直觉：Agent ≠ LLM + 工具调用

### 1.1 你的直觉是什么？

当你第一次听说 AI Agent 时，你脑海里浮现的画面大概率是这样的：

```
用户输入 → LLM 思考 → 调用工具 → 拿到结果 → LLM 再思考 → 给出最终答案
```

你会觉得，Agent 不过就是一个 **LLM 配上几个工具**。给它一个 while 循环，让模型反复调用工具直到完成任务 —— 这不就是 Agent 吗？

**这个直觉是错的。而且错得很致命。**

### 1.2 第一个公式：Agent = Model + Harness

在整个课程中，我们将反复回到这个公式：

```
Agent = Model + Harness
```

| 组件 | 角色 | 类比 | 决定什么 |
|------|------|------|----------|
| **Model** | 引擎 | 汽车发动机 | 决定**上限**（智能能达到的最高水平） |
| **Harness** | 驾驭系统 | 底盘+刹车+方向盘+仪表盘 | 决定**下限**（智能能稳定发挥的最低水平） |

一个残酷的事实是：**引擎再强，没有底盘和刹车，跑不过 100 公里就会车毁人亡。**

同样，一个再强大的模型（哪怕是 GPT-5、Claude Opus 4.6），如果只用朴素 while 循环去驱动，它会在以下情况中**系统性崩溃**：

- 对话超过 30 分钟 → Context 撑爆
- 工具调用失败了一次 → Agent 浑然不觉继续瞎编
- 进程被 kill → 之前所有进度全部丢失
- 遇到需要人工审批的危险操作 → 直接执行，无可挽回
- 循环卡死在某一步 → 永远出不来，直到 API 费用耗尽

### 1.3 引擎与底盘：一个汽车的隐喻

想象你要从北京开到拉萨，全程 3500+ 公里。

- 你有一台 **F1 赛车的发动机**（最强大的 LLM 模型）
- 但你没有底盘，没有刹车，没有方向盘，没有仪表盘，没有油箱

问题来了：**你能开多远？**

答案是：**最多 100 公里**。你会在以下时刻依次失败：

1. **第一次颠簸**（Tool 调用返回异常）→ 你没办法感知，继续踩油门
2. **连续开了 3 小时**（Context 溢出）→ 你的"记忆"满了，开始忘掉前面的路
3. **天黑之后**（没有状态持久化）→ 一旦熄火（crash），你连自己开到哪了都不知道
4. **下坡路段**（危险操作）→ 没有刹车，直接冲下悬崖

这就是 **Naive Agent** 的宿命。

---

## 二、Naive Agent：你以为的 Agent 长这样

### 2.1 最小可行 Agent 代码

下面这段代码，就是绝大多数人对 Agent 的全部想象。请把它读三遍，因为接下来的整章都在拆解它：

```python
import json

# ============================================
# 这就是你直觉中的 "Agent"
# ============================================

# 初始消息列表
messages = [
    {"role": "system", "content": "You are a helpful assistant with tool access."}
]

# 接收用户任务
user_task = input("请输入任务: ")
messages.append({"role": "user", "content": user_task})

# --- 核心循环：这就是你想象中的 Agent 全部 ---
while True:
    # ❌ 失效点 1 (循环失控): 没有最大迭代次数限制
    # ❌ 失效点 2 (Context溢出): messages 无界增长，永不压缩
    # ❌ 失效点 3 (Cache Miss): system prompt 位置会因 messages 增长而偏移

    # 调用 LLM
    response = llm.chat.completions.create(
        model="claude-sonnet-4-6",
        messages=messages,       # ← 整个 message 列表塞进去
        tools=tool_definitions   # ← 所有工具定义每次附带
    )

    assistant_message = response.choices[0].message

    # 检查是否有工具调用
    if assistant_message.tool_calls:
        # 把模型的工具调用请求加入 messages
        messages.append({
            "role": "assistant",
            "content": assistant_message.content,
            "tool_calls": assistant_message.tool_calls
        })

        # 逐个执行工具
        for tool_call in assistant_message.tool_calls:
            tool_name = tool_call.function.name
            tool_args = json.loads(tool_call.function.arguments)

            # ❌ 失效点 6 (缺权限闸): 没有任何权限检查
            #    删除文件、执行 shell 命令、修改数据库 —— 一律放行
            # ❌ 失效点 5 (状态丢失): 没有持久化当前步骤的进度
            #    如果此时进程 crash，所有已完成的操作记录全部丢失

            try:
                result = execute_tool(tool_name, **tool_args)
            except Exception as e:
                # ❌ 失效点 4 (Tool错误吞并):
                #    异常被吞！返回空字符串 ""
                #    模型收到的结果是 "" —— 它以为工具执行成功了
                #    然后基于错误假设继续推理，进入幻觉螺旋
                result = ""

            # 把工具执行结果塞回 messages
            messages.append({
                "role": "tool",
                "tool_call_id": tool_call.id,
                "content": str(result)   # ← 即使 result="" 也照塞不误
            })

    else:
        # 没有工具调用 → 认为任务完成
        # ❌ 失效点 7 (缺自动化评审):
        #    Agent 说 "做好了" 就真的做好了吗？
        #    没有人（也没有机制）去验证
        # ❌ 失效点 8 (成本失控):
        #    整个过程中没有任何 token 计数和预算控制
        print(assistant_message.content)
        break

# ============================================
# 这个 Agent 在真实任务中，存活时间 < 30 分钟
# ============================================
```

### 2.2 这段代码的朴素假设

上面的代码建立在 **五个致命假设** 上：

| # | 假设 | 为什么是错的 |
|---|------|-------------|
| 1 | **LLM 永远返回有意义的结果** | 模型会幻觉、会卡死、会输出格式错误 |
| 2 | **工具调用总是成功** | 网络超时、权限不足、参数错误、API 变更……失败才是常态 |
| 3 | **messages 列表可以无限增长** | 每个模型都有上下文窗口上限（200K tokens 也经不起无节制增长） |
| 4 | **进程永远不会 crash** | OOM、断电、网络断开、API 限流……中断随时发生 |
| 5 | **Agent 知道什么时候该停** | 模型倾向于"过度服务"，会反复修改、过度优化、永不满足 |

---

## 三、八种失效形态：逐一致命解剖

下面是 Naive Agent 的 **八种失效形态** 的完整解析。每种失效都包含：
- **发生位置**：在代码的哪个环节触发
- **故障原因**：为什么朴素实现必然遇到这个问题
- **后果**：如果不解决，Agent 会怎样

### 3.1 失效 1：循环失控 (Loop Runaway)

**发生位置**：`while True:` （主循环入口）

**故障原因**：
LLM 没有内在的"完成感"。当你给它工具时，它倾向于**持续使用工具**。一个典型的循环失控场景：

```
第 1 轮：我写了一个函数 → 调用工具测试 → 有一个小问题
第 2 轮：我修好了问题 → 调用工具测试 → 又发现一个优化点
第 3 轮：我做了优化 → 调用工具测试 → 还有一个边缘情况
...
第 47 轮：还在微调第三个函数的命名风格
```

这被称为 **过度服务循环 (Over-Service Loop)** 或 **工具成瘾 (Tool Addiction)**。

**后果**：
- 任务永远完不成
- API 费用持续燃烧
- 用户等到失去耐心，手动 kill 进程

**朴素代码中为什么没防住**：`while True` 就是字面意思 —— 永远的 True，直到 LLM 恰好返回一个没有 tool_calls 的响应。但在复杂任务中，模型**几乎总是**能找到"再做一件事"的理由。

---

### 3.2 失效 2：Context 溢出 (Context Overflow)

**发生位置**：`messages.append(...)` （每次循环都在追加，永不清理）

**故障原因**：
每次工具调用都会在 messages 中增加至少 2 条记录（assistant tool_call + tool result）。假设每轮迭代产生 2000 tokens，经过 100 轮迭代就是 200,000 tokens —— 已超过大多数模型的上下文窗口。

更糟的是，**工具返回内容不可控**。一次 `cat` 命令可能返回 10 万行日志，瞬间撑爆上下文。

**具体计算**：
```
假设模型上下文窗口 = 200K tokens
每轮迭代平均产生: 3000 tokens (assistant + tool results)
最大迭代次数 ≈ 200,000 / 3,000 ≈ 66 轮
但前 20 轮可能已经在做无意义重复劳动了
```

**后果**：
- 超过上下文窗口 → API 返回错误
- 接近窗口上限 → 模型开始"遗忘"早期指令 → 行为异常
- 如果使用滑动窗口截断 → 关键信息可能被裁掉

---

### 3.3 失效 3：Cache Miss (Prompt Cache 命中失败)

**发生位置**：`llm.chat.completions.create(model=..., messages=messages, ...)`

**故障原因**：
大多数 LLM API（包括 Anthropic 的 Claude API）提供 **Prompt Cache** 机制：如果请求的某个前缀与上一次请求相同，该部分不会被重新计算，费用降低 90%，延迟降低 80%。

但 Cache 的命中规则是**前缀匹配** —— 只对 messages 数组的**最前面**部分生效。如果 system prompt 后面不断追加新消息，system prompt 本身可以被缓存。然而：

1. 如果 system prompt 本身在变化（例如动态注入当前时间、进度信息）→ Cache 命中失败
2. 如果 messages 前几轮发生了修改/压缩 → Cache 命中失败
3. 如果 system prompt 的位置不是 messages[0] → Cache 命中失败

Naive Agent 完全没有考虑这些问题。

**后果**：
- 每次 API 调用的费用是全价，没有任何折扣
- 在一个长任务中，cache miss 可能导致费用增加 **5-10 倍**
- 延迟也显著增加

---

### 3.4 失效 4：Tool 错误吞并 (Tool Error Swallowing)

**发生位置**：`except Exception as e: result = ""`

**故障原因**：
这是最阴险的失效模式之一。工具调用失败有无数种可能：

```python
# 工具可能因为以下任何原因失败：
- 网络超时 (httpx.TimeoutException)
- 权限不足 (PermissionError)
- 文件不存在 (FileNotFoundError)
- API 返回 429 (RateLimitError)
- 参数格式错误 (TypeError, ValueError)
- 磁盘满 (OSError)
- 子进程被 kill (subprocess.CalledProcessError)
- 输出超过限制 (OutputTruncatedError)
```

当 `except Exception: result = ""` 把所有这些情况统一处理为 `""` 时，模型收到的信息是：

> "工具执行完毕，返回了空字符串"

模型会自然地推断：**"哦，操作成功了，只是没输出。"** 然后它会基于这个错误假设继续推理。

**具体危害链**：

```
1. Agent 调 delete_file("important_config.json") → 因为权限不足失败
2. except 吞掉异常 → result = ""
3. messages 追加: tool_result = ""
4. 模型看到空结果 → 推理 "文件已删除"
5. 模型继续第 5 步 → 基于"文件已不存在"来生成新配置文件
6. 创建了新配置文件 → 但旧文件还在 → 系统出现两个配置，行为不确定
```

**后果**：
- **幻觉螺旋 (Hallucination Spiral)**：Agent 在错误事实上继续推理，产生更多错误
- **静默失败 (Silent Failure)**：用户不知道哪里坏了，Agent 也不知道
- **排错极难**：回溯日志时，你只能看到一连串 `""`，无法定位故障点

---

### 3.5 失效 5：状态丢失 (State Loss)

**发生位置**：整个运行过程 —— 没有任何持久化

**故障原因**：
Agent 的所有状态（包括已完成的步骤、中间产物、决策记录）全部存在于**内存中**：

```python
messages = [...]        # 在内存里
current_progress = {}   # 也在内存里
intermediate_results = []  # 都在内存里
```

一旦进程终止（无论什么原因），**一切归零**。

**常见导致进程终止的场景**：
- 用户关闭终端 / IDE
- 系统休眠 / 重启
- OOM Killer 杀死进程
- API 限流导致程序异常退出
- 代码自身 bug 导致 crash
- 持续运行超过云函数的最大执行时间（如 AWS Lambda 的 15 分钟限制）

**后果**：
- 一个运行了 45 分钟、已完成 90% 的任务 → **从零开始**
- 用户产生强烈的挫败感和不信任
- 重复执行导致费用翻倍

---

### 3.6 失效 6：缺权限闸 (Missing Permission Gate)

**发生位置**：`execute_tool(tool_name, **tool_args)` —— 没有任何前置检查

**故障原因**：
Naive Agent 对工具调用**来者不拒**。模型说删文件就删文件，模型说执行 shell 命令就执行 shell 命令。

**危险操作清单**（这些在 Naive Agent 中全部畅行无阻）：
- `rm -rf /` 或 `rm -rf ~`
- `git push --force origin main`
- `DROP TABLE users;`
- `curl ... | bash`
- 读取 `~/.ssh/id_rsa` 或 `.env`
- 修改系统配置文件

**后果**：
- 最轻：删错了文件，需要从备份恢复
- 中等：代码被 force push，覆盖了同事的工作
- 严重：数据库被清空，生产环境崩溃
- 灾难：敏感信息泄露，安全事故

> **这不是杞人忧天。** 2024-2025 年间，多家 AI Coding Agent 产品都发生过因缺少权限闸导致用户代码被误删的事件。

---

### 3.7 失效 7：缺自动化评审 (Missing Automated Review)

**发生位置**：`print(assistant_message.content); break` —— 以模型的自我判断为唯一准绳

传统 Native Agent 以“不再调用 Function Call”作为判断任务完成的核心机制，源于其 **“思考-行动”的循环运行模式**。在这个循环中，“行动”被具体化为“调用工具”，因此“停止行动”便成了宣告“思考完成”的自然信号。

**故障原因**：
Naive Agent 判断任务完成的唯一标准是：**LLM 不再调用工具了**。

但 LLM 的自我判断极不可靠：

```
场景 A：Agent 被要求"写一个登录页面"
- Agent 写了 HTML → 没调用工具了 → "做好了！"
- 实际上：HTML 缺少 CSS、JS 逻辑有 bug、表单提交到不存在的地址

场景 B：Agent 被要求"修复数据库迁移的 bug"
- Agent 改了 migration 文件 → 没调用工具了 → "修好了！"
- 实际上：改完的 migration 在新数据库上跑不过

场景 C：Agent 被要求"优化 API 性能"
- Agent 加了缓存 → 没调用工具了 → "优化了 10 倍！"
- 实际上：没有 benchmark，10 倍这个数字是编的
```

**后果**：
- **交付质量全靠运气**：LLM 的态度 ≠ 产出的质量

- **用户必须二次验证**：你永远不敢直接信任 Agent 的"做好了"

- **复杂任务的失败率指数上升**：5 个步骤各 90% 成功率 → 整体 59% 成功率

  ——它把 **“模型是否认为自己完成了”** 当成了 **“任务是否真的高质量完成”**，两者在复杂任务中往往不相等

  ### 改进方向

  要解决这个问题，不能只靠“不再调用工具”，需要引入**显式的任务完成判断机制**，例如：

  - **专用终止工具**（如 `TerminateTool`）
    强制模型在认为完成后**显式调用 `finish(answer, confidence, summary)`**，模型必须输出完成声明和自检信息。这至少让“模型认为自己完成”这个动作变得可观测、可审计。
  - **反思 + 验证步骤**
    强制 Agent 在最终终止前执行一个“自省”或“验证”步骤（例如重新检查约束条件、重新计算关键值），并将验证结果作为是否终止的条件之一。
  - **外部评分器**
    引入一个独立的小模型或规则引擎，对 Agent 的中间产出和最终产出进行质量打分，只有分数超过阈值才允许终止。这就是一种 **“审批评判家”** 模式。
  - **基于计划的完成检测**
    要求 Agent 先生成执行计划（如步骤清单），在执行过程中勾选已完成步骤，只有计划中所有步骤都标记为完成且满足依赖关系，才允许终止。这能大幅减少“漏步”的情况。

---

### 3.8 失效 8：成本失控 (Cost Runaway)

**发生位置**：全局 —— 没有任何 token 预算机制

**故障原因**：
Naive Agent 的花费 = 每次 API 调用的 token 数 × API 调用次数。问题是：

1. **messages 在增长** → 每次调用的 token 数越来越大
2. **循环可能失控** → API 调用次数无上限
3. **Cache 不命中** → 每次都是全价
4. **没有预算上限** → 你设的 API key 限额是唯一防线

**费用增长曲线**（假设 Claude Sonnet $3/1M input tokens, $15/1M output tokens）：

```
第 1 轮:  messages ~2000 tokens → $0.006
第 10 轮: messages ~20000 tokens → $0.06
第 30 轮: messages ~60000 tokens → $0.18
第 50 轮: messages ~100000 tokens → $0.30
...
单次任务总费用: $3 - $15（取决于任务复杂度）
如果循环失控: 无上限
```

**后果**：
- 一个本应 $0.50 的任务，最终花掉 $15
- 月度 API 账单失控，企业和个人用户都难以预算
- 没有人会信任一个"不知道会花多少钱"的 Agent

---

## 四、失效时间线：从启动到崩溃

下面是一条典型的 Naive Agent 失效时间线。假设任务为「分析一个中型代码仓库并生成重构方案」。

```
T+0 min   🟢 Agent 启动
          messages = [system_prompt, user_task]
          
T+2 min   🟢 第 1-3 轮，一切正常
          Agent 读取文件，理解结构，制定计划
          
T+8 min   🟡 第 12 轮，首次工具调用失败
          网络抖动导致 API 超时
          ❌ FAILURE 4 (Tool错误吞并): except → result=""
          Agent 以为文件读取成功，实际上没读到内容
          
T+15 min  🟡 第 20 轮，幻觉螺旋开始
          基于空结果推理 → 对文件内容做出错误假设
          Agent 开始"修改"不存在的代码
          
T+25 min  🟠 第 30 轮，Context 接近窗口上限 80%
          messages 已达 160K tokens
          ❌ FAILURE 2 (Context溢出): 模型开始"遗忘"早期指令
          
T+30 min  🟠 第 35 轮，Cache Miss 持续
          每次请求都在重新计算整个前缀
          ❌ FAILURE 3 (Cache Miss): 费用累计已达 $5
          
T+35 min  🔴 第 40 轮，循环失控
          Agent 在修改第 8 个文件时反复微调
          ❌ FAILURE 1 (循环失控): 没有终止条件
          ❌ FAILURE 8 (成本失控): 费用越过 $10
          
T+40 min  🔴 第 42 轮，权限越界
          Agent 决定 "删除旧代码"
          没有权限检查 → 直接执行 rm
          ❌ FAILURE 6 (缺权限闸): 删除了尚未提交的新代码
          
T+42 min  💀 CRASH
          进程因 OOM 被杀（messages 列表占用过多内存）
          ❌ FAILURE 5 (状态丢失): 最后 42 分钟的工作全部丢失
          ❌ FAILURE 7 (缺评审): 即使不 crash，"产出"也无法使用
          
T+42 min  最终状态：
          - 花费: $12.50
          - 产出: 0（全部丢失）
          - 质量: 无法评估（没有验证过）
          - 用户情绪: 😤
```

**时间线图（ASCII）：**

```
Agent 质量
  100% ┤
       │  🟢        🟡       🟠          🔴      💀
   80% ┤          ·        ·           ·       ·
       │        ·        ·           ·       ·
   60% ┤      ·        ·     ↘     ·       ·
       │    ·        ·        ↘ ·       ·
   40% ┤  ·        ·           ↘
       │·        ·              ↓ Crash
   20% ┤        ·
       │      ·
    0% ┤────┼────────┼───────────┼──────────┼─────→ 时间
       0   10       20          30         40     (分钟)
       
       引擎还行    消息膨胀    幻觉蔓延    循环失控  全丢
```

---

## 五、同任务三模式演示：差距一目了然

### 5.1 任务描述

> **任务**：给定一个包含 5000 行销售数据的 CSV 文件，完成：
> 1. 读取并清洗数据
> 2. 按地区和季度计算销售汇总
> 3. 找出 Top 10 产品
> 4. 生成一份包含图表的分析报告（PDF）
> 5. 如果数据异常，说明可能原因并给出建议
>
> 这个任务需要约 12-15 个工具调用，涉及文件读写、Python 执行、图表生成、PDF 导出。

### 5.2 裸模型 LLM 的表现

```
用户: 请分析这个 CSV 文件...（上传了文件路径）

裸 LLM: 我无法访问你的文件系统。请你把 CSV 的前 20 行粘贴给我。

用户: （粘贴了 20 行数据）

裸 LLM: 根据你提供的前 20 行数据，我推测整体趋势是...（基于不完整数据推断）

用户: 能生成图表吗？

裸 LLM: 我无法直接生成图表，但我可以给你 matplotlib 代码，
       你复制到本地运行即可。代码如下：
       ```python
       import matplotlib...
```

结果：
✅ 给了有用的 Python 代码
❌ 无法读文件 —— 只能靠用户粘贴部分数据
❌ 无法执行代码 —— 代码是否运行正确完全未知
❌ 无法生成 PDF —— 没有文件系统写入权限
❌ 无法完成端到端任务 —— 用户需要自己做大量手工工作
```

### 5.3 LLM + Code Interpreter 的表现

```
用户: 请分析这个 CSV 文件...

LLM+CI: 让我读取文件。
[调用 read_file] ✅ 成功读取 5000 行

LLM+CI: 数据已读取。让我清洗和分析。
[调用 python_exec] 
```python
import pandas as pd
df = pd.read_csv('sales.csv')
df_clean = df.dropna()...
```
✅ 代码执行成功

LLM+CI: 清洗完成。汇总数据如下：
[展示表格]

LLM+CI: 让我生成图表。
[调用 python_exec]
```python
import matplotlib.pyplot as plt
...
plt.savefig('chart.png')
```
✅ 图表生成

LLM+CI: 现在生成 PDF 报告。
[调用 python_exec]
```python
from reportlab import ...
```
✅ PDF 基本生成

结果：
✅ 可以读文件
✅ 可以执行 Python 代码
✅ 可以生成图表和 PDF
⚠️ 如果中间某步出错，恢复能力有限
❌ 无法跨多个工具协作（只有 Python 沙箱）
❌ 长时间任务中 context 管理简单粗暴
❌ 没有权限闸（沙箱内任意操作）
```

### 5.4 Claude Code (LLM + Harness) 的表现

```
用户: 请分析这个 CSV 文件...

Claude Code: 
  → [上下文工程] 读取 CLAUDE.md 了解项目规范
  → [任务拆解] 把任务拆成 5 个子任务，写入 progress.json
  → [四相循环 第1轮]
     - Gather: 读取 CSV 文件、了解数据结构
     - Act: 执行 Python 清洗脚本
     - Verify: 检查清洗后的行数是否正确
     - Iterate: 发现 3 行异常数据，标记待处理
  → [四相循环 第2轮]
     - Gather: 确认异常数据的原因（货币单位不一致）
     - Act: 统一货币单位，重跑清洗
     - Verify: 用 assert 验证数据一致性
  → [进度跟踪] 第 1-2 步完成 → 记录到 progress.json
  → [子代理] 派生子 Agent 去分析 Top 10 产品
  → [四相循环 第3轮]
     - Act: 生成图表
     - Verify: 用 Playwright 打开 HTML 预览，截图确认
  → [四相循环 第4轮]
     - Act: 生成 PDF 报告
     - Verify: 检查 PDF 文件大小和页数 → 通过
  → [上下文管理] messages 接近阈值 → 压缩中间过程
  → [GAN 评审] Evaluator Agent 独立审查报告质量 → 通过

✅ 任务完成，耗时 8 分钟，花费 $1.20

结果：
✅ 读文件、执行代码、生成图表/PDF —— 全部完成
✅ 跨工具编排 —— 文件系统 + Python + 浏览器 + PDF 工具协同
✅ 任务拆解 —— 大任务化小，逐步推进
✅ 错误恢复 —— 发现数据异常后自动修正
✅ 状态持久化 —— 每一步进度落盘，crash 可恢复
✅ 权限管控 —— 危险操作需用户确认
✅ 质量验证 —— Playwright 真机测试，Evaluator 独立评审
✅ 上下文管理 —— 自动压缩，避免溢出
```

### 5.5 三层次能力对比矩阵

| 能力维度 | 裸 LLM | LLM + Code Interpreter | **Claude Code (LLM + Harness)** |
|----------|:------:|:----------------------:|:-------------------------------:|
| **读取文件** | ❌ | ✅ | ✅ |
| **执行代码** | ❌ | ✅ (沙箱内) | ✅ (全环境) |
| **跨工具编排** | ❌ | ❌ (仅 Python) | ✅ (文件/Shell/浏览器/数据库...) |
| **持续 30 分钟+ 对话** | ❌ (Context 爆) | ⚠️ (勉强) | ✅ (上下文工程) |
| **任务失败补救** | ❌ | ⚠️ (手动重试) | ✅ (自动检测+修正) |
| **断点续传** | ❌ | ❌ | ✅ (Progress Tracking) |
| **权限管控** | ❌ | ⚠️ (沙箱隔离) | ✅ (Permission Gate) |
| **自动化质量评审** | ❌ | ❌ | ✅ (Verify Loop + Evaluator) |
| **Token 成本控制** | ❌ | ❌ | ✅ (Token Budget) |
| **子任务并行** | ❌ | ❌ | ✅ (Subagent) |
| **Prompt Cache 优化** | ❌ | ❌ | ✅ (稳定前缀) |
| **终端到终端** | ❌ | ⚠️ | ✅ |

**关键洞察**：从 LLM + Code Interpreter 到 Claude Code 之间的差距，**不是模型能力的差距**（可能在用同一个模型），而是 **Harness 的差距**。

---

## 六、三代工程：演进而非替代

驾驭工程不是凭空出现的，它是 AI 工程实践演进的第三阶段。

### 6.1 第一代：提示词工程 (Prompt Engineering)

**核心问题**：**如何精确表达意图？**

```
时代背景: GPT-3 (2020), GPT-3.5/ChatGPT (2022)
模型形态: 单轮对话，没有工具调用
工程焦点: 怎么把话说清楚
```

**代表技术**：
- Few-shot prompting（给模型几个例子）
- Chain-of-Thought（让模型一步步推理）
- Role prompting（"你是一个专业的..."）
- 模板化 prompt（Mad Libs 式的填空）

**局限性**：
- 纯文本入，纯文本出——模型无法与外部世界交互
- 遇到知识截断日期后的事情 → 只能猜
- 无法执行操作（不能发邮件、不能查数据库）

### 6.2 第二代：上下文工程 (Context Engineering)

**核心问题**：**模型能看到什么？**

```
时代背景: GPT-4 (2023), Claude 2/3 (2023-2024)
模型形态: 多轮对话，工具调用 (Function Calling / Tool Use)
工程焦点: 把什么信息放进上下文窗口
```

**代表技术**：
- RAG（检索增强生成——把相关文档放入上下文）
- 对话历史管理（多轮对话的记忆）
- 工具定义与结果格式化（Tool Use schema 设计）
- System prompt 工程（行为约束和角色定义）
- 动态上下文注入（当前时间、用户信息、环境变量等）

**核心理念**：模型的能力边界由它"能看见什么"决定。你给它检索工具，它就能看见知识库；你给它浏览器工具，它就能看见网页；你给它代码执行器，它就能看见运行结果。

**局限性**：
- 仍然是被动的"调用-响应"模式
- 缺乏对 Agent **行为**的系统性约束
- 没有解决"Agent 跑着跑着就崩了"的问题
- 每次对话是独立的、隔离的

### 6.3 第三代：驾驭工程 (Harness Engineering)

**核心问题**：**如何让 Agent 持续、稳定、安全地运行？**

```
时代背景: Claude Code (2025), Cursor Agent, Devin, DeepAgents
模型形态: 自主 Agent，长时运行，多工具协同
工程焦点: 系统性约束与治理
```

**核心能力**：

| 维度 | 要解决的问题 | 代表机制 |
|------|------------|---------|
| **持续运行** | Agent 怎么跑 30 分钟不崩？ | 四相循环, 上下文管理, Token 预算 |
| **稳定产出** | Agent 每次都能高质量交付？ | 验证循环, GAN 三角色评审 |
| **安全边界** | Agent 不会做危险的事？ | 权限闸, Hook 系统 |
| **可恢复性** | 进程 crash 后怎么办？ | 进度跟踪, Checkpoint |
| **成本可控** | 不会烧掉无限 API 费用？ | Token 预算, Cache 优化 |
| **可观察性** | 你能看到 Agent 在干什么？ | 日志, 审计追踪, 状态面板 |

**架构特征**：
- 不再是单层 while 循环，而是**多层嵌套的工程系统**
- 模型是系统的一部分，不是系统的全部
- 每一层都有防御性设计（fail-safe，不是 fail-fast）
- 从"信任模型"转向"验证模型"

### 6.4 框架演进：LangChain → LangGraph → DeepAgents

框架/工具的发展也忠实反映了三代工程的演进：

```
LangChain (2022-2023)
├── 本质: 提示词工程 + 工具调用的框架化
├── 核心抽象: Chain, Agent, Tool
├── 解决了: "如何用代码组织 LLM 调用链"
└── 问题: 线性链不可控，状态管理混乱

        ↓ 演进：从框架 → 运行时

LangGraph (2024)
├── 本质: 上下文工程 + 流程编排的运行时
├── 核心抽象: Graph, Node, Edge, State
├── 解决了: "如何让 Agent 流程可控、可分支、可循环"
└── 问题: 仍然需要大量手写控制逻辑

        ↓ 演进：从运行时 → 驾驭

DeepAgents (2025)
├── 本质: 驾驭工程的 Agent 框架
├── 核心抽象: Subagent, TodoList, FileSystem, Hooks
├── 解决了: "如何让 Agent 长期自主运行而不崩"
└── 关键机制: 任务拆解、进度持久化、子代理隔离
```

**演进的核心规律**：**包含而非替代**。

- 驾驭工程仍然需要上下文工程（你要给模型放什么信息进去）
- 上下文工程仍然需要提示词工程（你放进去的信息需要精确表达）
- 每一代是上一代的**扩展**和**增强**，不是推翻重来

---

## 七、本章小结

1. **Agent ≠ LLM + 工具调用**。这个直觉会让你在真实任务中反复碰壁。

2. **Agent = Model + Harness**。
   - Model 决定上限（能做到多好）
   - Harness 决定下限（能稳定做到多好）
   - 上限靠模型，下限靠工程。

3. **Naive Agent 的 while 循环会在 8 个点上系统性崩溃**：
   - 循环失控 → 永不终止
   - Context 溢出 → 记忆撑爆
   - Cache Miss → 费用飙升
   - Tool 错误吞并 → 幻觉螺旋
   - 状态丢失 → crash 归零
   - 缺权限闸 → 危险操作畅行无阻
   - 缺自动化评审 → 产出不可信
   - 成本失控 → 账单爆炸

4. **三种模式的对比直接证明了 Harness 的价值**：同一个模型，加上 Harness 就能完成裸模型无法企及的任务。

5. **三代工程**——提示词工程 → 上下文工程 → 驾驭工程——不是替代关系，而是逐层叠加的关系。

> **下一章 [第二幕 · 立](./02-立-八大核心机制与三支柱.md)**，我们将针对这八种失效形态，逐一建立解决方案，构建 Harness Engineering 的核心机制坐标体系。
