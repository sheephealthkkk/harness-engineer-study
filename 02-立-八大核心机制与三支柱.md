# 第二幕 · 立 —— 建立坐标体系，八大机制与三支柱

> **核心目标**：针对 Naive Agent 的八种失效形态，建立一一对应的八大核心解决机制；
> 从三支柱（输入流、控制流、状态流）的正交视角俯瞰整个 Harness 系统；
> 通过两张正交对照矩阵，形成完整的驾驭工程知识坐标。

---

## 目录

- [一、问题-解决全景图](#一问题-解决全景图)
- [二、八大核心机制详解](#二八大核心机制详解)
  - [2.1 四相循环 (4-Phase Loop) —— 解决循环失控](#21-四相循环-4-phase-loop--解决循环失控)
  - [2.2 工具编排 (Tool Orchestration) —— 解决 Tool 错误吞并](#22-工具编排-tool-orchestration--解决-tool-错误吞并)
  - [2.3 进度跟踪 (Progress Tracking) —— 解决状态丢失](#23-进度跟踪-progress-tracking--解决状态丢失)
  - [2.4 上下文工程 (Context Engineering) —— 解决 Context 溢出 + Cache Miss](#24-上下文工程-context-engineering--解决-context-溢出--cache-miss)
  - [2.5 任务拆解 (Task Decomposition) —— 解决 Context 溢出的根源](#25-任务拆解-task-decomposition--解决-context-溢出的根源)
  - [2.6 验证循环 (Verify Loop) —— 解决 Tool 错误吞并（任务层面）](#26-验证循环-verify-loop--解决-tool-错误吞并任务层面)
  - [2.7 子代理分治 (Subagent Delegation) —— 解决 Context 溢出的另一维度](#27-子代理分治-subagent-delegation--解决-context-溢出的另一维度)
  - [2.8 GAN 启发三角色架构 —— 解决缺自动化评审](#28-gan-启发三角色架构--解决缺自动化评审)
- [三、三支柱正交视角](#三三支柱正交视角)
  - [3.1 输入流 (Input Flow) —— Agent 能看见什么](#31-输入流-input-flow--agent-能看见什么)
  - [3.2 控制流 (Control Flow) —— Agent 能干什么](#32-控制流-control-flow--agent-能干什么)
  - [3.3 状态流 (State Flow) —— Agent 能跑多久](#33-状态流-state-flow--agent-能跑多久)
- [四、两张正交对照矩阵](#四两张正交对照矩阵)
  - [4.1 矩阵一：八大失效 × 八大机制](#41-矩阵一八大失效--八大机制)
  - [4.2 矩阵二：八大机制 × 三支柱](#42-矩阵二八大机制--三支柱)
- [五、本章小结](#五本章小结)

---

## 一、问题-解决全景图

在 [第一幕](./01-破-驾驭工程原理与概念.md) 中，我们识别了 Naive Agent 的八种失效形态。
现在我们为每一种失效建立对应的解决机制：

```
Naive Agent 的八种失效                     Harness 的八大机制
─────────────────────────              ────────────────────────
                                      
① 循环失控 ─────────────────────→ 四相循环 (4-Phase Loop)
   永远停不下来                        每次迭代经过 4 个阶段，强制收敛

② Context 溢出 ─────────────┬──→ 上下文工程 (Context Engineering)
   messages 撑爆窗口          │     监控 tokens，压缩/重置，保持前缀稳定
                             │
③ Cache Miss ───────────────┘     稳定 system prompt 前缀，最大化命中

④ Tool 错误吞并 ───────┬────→ 工具编排 (Tool Orchestration)
   异常变空串，幻觉螺旋  │       结构化错误返回，让 Agent 看见失败
                        │
                        └────→ 验证循环 (Verify Loop)
                               真机验证，不信任 Agent 的自我判断

⑤ 状态丢失 ─────────────────────→ 进度跟踪 (Progress Tracking)
   crash = 全部归零                   每步持久化到文件，git commit 里程碑

⑥ 缺权限闸 ─────────────────────→ 权限闸 (Permission Gate)
   危险操作畅行无阻                   分层权限系统 + Hook + 人工审批

⑦ 缺自动化评审 ─────────────────→ GAN 三角色架构
   Agent 说 "好了" ≠ 真的好了          Planner + Generator + Evaluator

⑧ 成本失控 ─────────────────────→ Token 预算 (Token Budget)
   账单无上限                         阈值触发收敛/压缩/终止
```

**关键设计原则**：这不是给 Naive Agent 打 8 个补丁，而是**重新设计 Agent 的运行时架构**。
八大机制互相配合，形成完整的驾驭系统。

---

## 二、八大核心机制详解

### 2.1 四相循环 (4-Phase Loop) —— 解决循环失控

**一句话总结**：把一个朴素的"调用-执行"循环改造成 4 阶段的结构化迭代，每轮强制收敛。

#### 2.1.1 什么是四相循环？

Naive Agent 的循环：
```
while True: LLM → Tools → LLM → Tools → ... → (永不终止)
```

四相循环：
```
for iteration in range(MAX_ITERATIONS):
    phase 1: GATHER   —— 收集上下文（我该看什么？）
    phase 2: ACT      —— 执行动作（我该做什么？）
    phase 3: VERIFY   —— 验证结果（做对了吗？）
    phase 4: ITERATE  —— 迭代改进（还要做什么？如果完成 → break）
```

#### 2.1.2 四相详解

**Phase 1: GATHER (收集上下文)**

```
目标：确保 Agent 在行动前有足够的信息
操作：读取文件、检索文档、查看前一步结果
产出：对当前状态的清晰理解
```

这一阶段强制 Agent **先看再动**。Naive Agent 经常在信息不足时就动手 —— 比如还不知道文件结构就开始修改代码。Gather 阶段让 Agent 先回答"我目前知道什么？我还需要知道什么？"

**Phase 2: ACT (执行动作)**

```
目标：执行一个具体的、原子化的操作
操作：写文件、运行命令、调用 API
产出：一个可验证的结果（文件已修改、命令已执行）
```

这一阶段要求动作是**原子的**，即做一件明确的事。不是"重构整个模块"而是"重命名 UserService 类的 getUserName 方法为 getUsername"。

**Phase 3: VERIFY (验证结果)**

```
目标：确认行动产生了预期的效果
操作：运行测试、检查 linter、用 Playwright 截图、检查文件哈希
产出：验证通过 / 验证失败 + 失败原因
```

这是四相循环的心脏。**每一步都验证**，不是做完所有事再验证。如果验证失败，进入 Phase 4；如果验证通过且任务完成，退出循环。

**Phase 4: ITERATE (迭代改进)**

```
目标：基于验证结果决定下一步
操作：
  - 验证通过 + 还有任务 → 回到 Phase 1，处理下一个子任务
  - 验证失败 → 分析原因，回到 Phase 1，修正后重试
  - 全部完成 → break
  - 达到最大迭代次数 → 强制终止 + 报告未完成项
```

#### 2.1.3 关键设计：最大迭代次数 + 终止条件

```python
MAX_ITERATIONS = 25  # 硬上限

# 额外终止条件：
- 连续 3 轮验证失败 → 暂停，请求人工介入
- 连续 5 轮没有产生实质变化 → 判定为卡死，终止
- Token 预算耗尽 → 优雅降级终止
```

#### 2.1.4 四相循环伪代码

```python
def four_phase_loop(task, max_iterations=25):
    progress = load_progress()  # 从持久化文件恢复（见 2.3）
    iteration = progress.get('iteration', 0)
    
    while iteration < max_iterations:
        # === PHASE 1: GATHER ===
        context = gather_information(task, progress)
        # 收集：已完成的子任务、当前的环境状态、相关文件内容
        
        # === PHASE 2: ACT ===
        result = execute_action(task, context, progress)
        # 执行：一个原子操作（修改一个函数、运行一个命令）
        
        # === PHASE 3: VERIFY ===
        verification = verify_result(result, task)
        # 验证：运行测试、检查输出、真机验证
        
        if verification.failed:
            # === PHASE 4: ITERATE (失败路径) ===
            progress.record_failure(verification.reason)
            iteration += 1
            continue  # 回到 Phase 1，修正问题
        
        # === PHASE 4: ITERATE (成功路径) ===
        progress.mark_completed(result.subtask_id)
        
        if progress.all_completed:
            break  # 所有子任务完成
        
        iteration += 1
    
    return progress.summary()
```

#### 2.1.5 解决了什么问题？还有什么问题？

| 解决的问题 | 解决方式 |
|-----------|---------|
| 无限循环 | 硬上限 + 连续失败终止 |
| 盲目行动 | 强制 Gather 阶段，先看再动 |
| 产出不可靠 | Verify 阶段，每步验证 |

| 残余问题 | 需要配合的其他机制 |
|---------|------------------|
| 大任务的单步仍然很重 | 配合任务拆解 (2.5) |
| 验证工具的可靠性 | 配合验证循环 (2.6) |
| 四相循环自身也产生 tokens | 配合上下文工程 (2.4) |

---

### 2.2 工具编排 (Tool Orchestration) —— 解决 Tool 错误吞并

**一句话总结**：所有工具调用包裹在结构化 Schema 中，成功返回结果，失败返回结构化错误信息，绝不让 Agent 看见一个空字符串。

#### 2.2.1 Naive 做法 vs 编排做法

**Naive 做法**：
```python
try:
    result = execute_tool(name, args)
except Exception:
    result = ""  # ← 吞并！Agent 不知道发生了什么
```

**编排做法**：
```python
def orchestrated_tool_call(name, args):
    try:
        # 前置：权限检查（见 2.2.4）
        if not permission_gate.check(name, args):
            return ToolResult(
                success=False,
                error_type="PERMISSION_DENIED",
                error_message=f"操作 {name} 需要人工审批",
                suggestion="请向用户请求确认后再执行"
            )
        
        # 执行
        raw_result = execute_tool(name, args)
        
        return ToolResult(
            success=True,
            data=raw_result,
            metadata={
                "duration_ms": elapsed,
                "tokens_consumed": len(str(raw_result)),
                "tool_version": TOOL_VERSIONS[name]
            }
        )
    except TimeoutError:
        return ToolResult(
            success=False,
            error_type="TIMEOUT",
            error_message=f"工具 {name} 执行超时（>30s）",
            suggestion="请尝试减少数据量或分割请求后重试"
        )
    except PermissionError:
        return ToolResult(
            success=False,
            error_type="PERMISSION_ERROR",
            error_message=f"没有权限执行 {name}",
            suggestion="请检查文件权限或切换到有权限的目录"
        )
    except Exception as e:
        return ToolResult(
            success=False,
            error_type="UNKNOWN_ERROR",
            error_message=str(e),
            error_traceback=traceback.format_exc()[-500:],  # 截断
            suggestion="这是一个未预期的错误，请报告给用户并尝试替代方案"
        )
```

#### 2.2.2 工具结果 Schema

```python
class ToolResult:
    success: bool                  # 成功/失败
    data: Any = None              # 成功时的数据
    error_type: str = None        # 失败时的错误分类
    error_message: str = None     # 失败时的人类可读错误描述
    error_traceback: str = None   # 失败时的技术细节（截断版）
    suggestion: str = None        # 给 Agent 的建议："你接下来应该怎么做"
    metadata: dict = {}           # 耗时、tokens、工具版本等元数据
```

**关键设计**：
- `success: bool` 让 Agent 一眼知道成败
- `error_type` 让 Agent 理解错误类别（TIMEOUT vs PERMISSION vs UNKNOWN），选择不同应对策略
- `suggestion` 是给 Agent 的"救援提示"——不是让它自己猜怎么修，而是告诉它"试试这个"
- `metadata` 为系统层面提供可观测性数据

#### 2.2.3 为什么这解决了"错误吞并"？

```
Naive (错误被吞):
  tool_result = "" (空字符串)
  → Agent 看到: "结果是空的，说明操作成功了只是没输出"
  → Agent 继续基于错误假设工作 ← 幻觉螺旋

Orchestrated (错误被结构化):
  tool_result = {
    "success": false,
    "error_type": "TIMEOUT",
    "error_message": "工具 read_large_file 执行超时（>30s）",
    "suggestion": "请尝试使用 offset 和 limit 分段读取文件"
  }
  → Agent 看到: "操作失败了，因为超时。建议用分段方式读取"
  → Agent 调整策略：用 offset/limit 分 3 次读取 ← 正确恢复
```

#### 2.2.4 与权限闸的协作

工具编排的 **前置检查** 是与权限闸的接口点：

```python
# 工具编排在执行前先调用权限闸
if permission_gate.requires_approval(name, args):
    # 暂停执行，等待用户审批
    approval = request_user_approval(name, args, risk_level)
    if not approval:
        return ToolResult(success=False, error_type="USER_DENIED", ...)
```

详细的权限闸设计见第 2.5 节（控制流）。

---

### 2.3 进度跟踪 (Progress Tracking) —— 解决状态丢失

**一句话总结**：每完成一个子任务里程碑，立即将进度持久化到本地 JSON 文件，关键里程碑打 git commit。Session 重启时自动读取进度文件，从断点续传。

#### 2.3.1 核心问题

```
没有进度跟踪的 Agent:
  T+0:   开始执行 10 步任务
  T+25:  完成 7 步（70% 进度）
  T+26:  💥 CRASH（OOM / 断电 / 网络断 / API 限流 / 用户关终端）
  T+27:  全部丢失。重跑需要从第 1 步开始。
         浪费了前 25 分钟的所有工作和 API 费用。

有进度跟踪的 Agent:
  T+0:   读取 progress.json → 发现上次崩在第 7 步
  T+1:   从第 8 步开始继续 ← 断点续传
  T+8:   完成剩余 3 步
```

#### 2.3.2 进度文件结构

```json
{
  "task_id": "task-2025-05-21-001",
  "task_description": "重构 UserService 模块",
  "created_at": "2025-05-21T10:00:00Z",
  "updated_at": "2025-05-21T10:25:00Z",
  "current_phase": "act",
  "total_subtasks": 10,
  "completed_subtasks": 7,
  "subtasks": [
    {
      "id": "sub-1",
      "description": "抽取 UserService 接口",
      "status": "completed",
      "completed_at": "2025-05-21T10:03:00Z",
      "git_commit": "a1b2c3d",
      "verification": "tests/user_service_test.py: 12 passed"
    },
    {
      "id": "sub-2",
      "description": "重命名 getUserName → getUsername",
      "status": "completed",
      "completed_at": "2025-05-21T10:07:00Z",
      "git_commit": "e4f5g6h",
      "verification": "grep 确认旧方法名 0 引用"
    },
    {
      "id": "sub-8",
      "description": "更新 EmailService 中的引用",
      "status": "in_progress",
      "attempts": 2,
      "last_error": "EmailService 在另一个模块中，需要跨模块导入"
    },
    {
      "id": "sub-9",
      "description": "运行全量回归测试",
      "status": "pending"
    },
    {
      "id": "sub-10",
      "description": "更新 CHANGELOG",
      "status": "pending"
    }
  ],
  "checkpoints": [
    {"at": "sub-3", "commit": "x1y2z3", "message": "接口抽取和重命名完成"},
    {"at": "sub-7", "commit": "p4q5r6", "message": "所有重命名和引用更新完成"}
  ]
}
```

#### 2.3.3 进度写入策略

```
写入时机：
  1. 每个子任务状态变更时（pending → in_progress → completed/failed）
  2. 每轮四相循环结束时
  3. 收到终止信号时（SIGTERM / SIGINT 的 graceful shutdown）
  4. 定时写入（每 30 秒一次，作为兜底）

Git Commit 时机：
  1. 每完成一个逻辑里程碑时（如 "接口抽取完成"）
  2. 子任务涉及文件修改且通过验证时
  3. 用户手动触发 checkpoint 时

设计原则：
  - 写入操作必须是原子的（先写 .tmp 再 rename）以防写入中途 crash
  - 每次写入记录版本号，防止并发写入冲突
  - Commit 消息包含子任务 ID，方便回溯
```

#### 2.3.4 恢复流程

```python
def resume_or_start(task_description):
    progress_file = Path(".claude/progress.json")
    
    if progress_file.exists():
        progress = json.loads(progress_file.read_text())
        
        # 检查是否过期（超过 24 小时的进度视为废弃）
        last_update = datetime.fromisoformat(progress["updated_at"])
        if datetime.utcnow() - last_update > timedelta(hours=24):
            print(f"⚠️ 发现超过 24 小时的旧进度，是否继续？")
            if not user_confirms():
                progress = create_new_progress(task_description)
        
        # 找到第一个未完成的子任务
        next_task = find_next_pending(progress)
        if next_task:
            print(f"🔄 从子任务 {next_task['id']} 继续执行")
            print(f"   已完成 {progress['completed_subtasks']}/{progress['total_subtasks']}")
        else:
            print("✅ 所有子任务已完成")
        
        return progress
    else:
        # 全新开始
        return create_new_progress(task_description)
```

#### 2.3.5 进度跟踪的扩展价值

除了断点续传，进度文件还提供：
- **可观测性**：用户打开 progress.json 就能看到"做到哪了"
- **审计追踪**：配合 git log，完整回溯 Agent 做了什么事、什么时候做的
- **协作**：理论上多个 Agent 可以通过进度文件协调分工（高级用法）
- **分析**：统计 Agent 完成各类子任务的平均耗时、失败率

---

### 2.4 上下文工程 (Context Engineering) —— 解决 Context 溢出 + Cache Miss

**一句话总结**：监控 messages 的 token 数量，超过阈值触发压缩或重置；保持 system prompt 前缀绝对稳定以最大化 Prompt Cache 命中。

#### 2.4.1 上下文工程的三个维度

```
上下文工程 = 放什么进去 × 怎么放进去 × 什么时候清出去

放什么 (Input Selection):   CLAUDE.md, 对话历史, 检索结果, 进度文件
怎么放 (Format & Order):     System 前缀稳定, 动态内容放后面
什么时候清 (Lifecycle):     压缩 (Compaction), 重置 (Reset)
```

#### 2.4.2 Token 监控与阈值

```python
class ContextManager:
    # 硬阈值：绝对不可超越（模型上下文窗口的 80%）
    HARD_LIMIT = 160_000  # 假设模型有 200K 窗口
    
    # 软阈值：触发压缩
    SOFT_LIMIT = 120_000
    
    # 警告阈值：提醒用户
    WARNING_LIMIT = 80_000
    
    def check_and_manage(self, messages: list) -> list:
        current_tokens = count_tokens(messages)
        
        if current_tokens > self.HARD_LIMIT:
            # 硬重置：保留 system prompt + 最后 N 轮交互
            return self.hard_reset(messages)
        
        if current_tokens > self.SOFT_LIMIT:
            # 软压缩：把中间过程总结为摘要
            return self.compact(messages)
        
        if current_tokens > self.WARNING_LIMIT:
            # 警告：通知 Agent 开始注意节约 token
            inject_warning("⚠️ Context 使用已达 50%，请精简后续输出")
        
        return messages
```

#### 2.4.3 压缩策略 (Compaction)

压缩不是简单的截断，而是**让模型自己总结中间过程**：

```python
def compact(messages):
    """
    策略：保留 system prompt + 前 3 轮 + 后 5 轮交互
    中间部分让一个轻量模型（如 Claude Haiku）对中间过程做摘要
    """
    system = messages[0]  # 保留 system prompt
    head = messages[1:6]  # 保留前 3 轮（用户任务 + 初始探索）
    middle = messages[6:-10]  # 中间过程（可能非常多轮）
    tail = messages[-10:]  # 保留最后 5 轮（最近的上下文最重要）
    
    if len(middle) > 0:
        # 调用轻量模型对中间过程做摘要
        summary = compact_model.summarize(middle)
        compacted = f"[上下文压缩：以下为中间 {len(middle)} 轮交互的摘要]\n{summary}"
        return [system] + head + [
            {"role": "user", "content": compacted}
        ] + tail
    
    return [system] + head + tail
```

**压缩的关键原则**：
- **System prompt 永远不动**（这是 Cache 命中的前提）
- **最近的交互最重要**（tail 权重最高，保留原始内容）
- **中间过程用摘要代替**（丢掉细节，保留结论和关键决策）
- **压缩信息对 Agent 透明**（Agent 知道自己是在压缩后的上下文中工作）

#### 2.4.4 Prompt Cache 优化

Anthropic 的 Prompt Cache 机制：如果请求的某个**前缀**与上一次请求完全相同，该前缀的计算会被缓存。缓存命中后：
- **价格降低 90%**
- **延迟降低 80%**

**最大化 Cache 命中的策略**：

```python
# ✅ 正确做法：System Prompt 固定在最前面，绝对不动
messages = [
    {"role": "system", "content": STABLE_SYSTEM_PROMPT},  # ← 这个始终不变
    # 后续消息尽量在最后面追加，不改动前面的内容
    ...existing_messages,
    {"role": "user", "content": new_content}  # 新内容只在尾部追加
]

# ❌ 错误做法：把 system prompt 改了
messages[0]["content"] = f"{STABLE_SYSTEM_PROMPT}\n当前时间：{datetime.now()}"
# ↑ 每次时间都不同 → 每次都不命中 Cache → 多花 90% 的钱

# ✅ 正确做法：动态信息放在 user message 中
messages.append({
    "role": "user", 
    "content": f"[系统状态] 当前时间：{datetime.now()}，进度：{progress}"
})
# ↑ System prompt 不变 → Cache 命中 → 省 90%
```

**Cache 友好的消息布局**：
```
messages = [
  [0] system: "...固定的 system prompt..."           ← Cache 断点 1
  [1] user: "任务描述..."                              ← Cache 断点 2
  [2] assistant: "我先读一下项目结构..."                 ← Cache 断点 3
  ...（最多 4 个断点）
  [N-2] assistant: "...最新的工具调用..."
  [N-1] tool: "...最新的工具结果..."
  [N] user: "...最新的用户消息..."
]
```

#### 2.4.5 重置策略 (Reset)

当压缩也不够时（context 实在太多了），需要更激进的策略：

```
L1 策略: 摘要压缩 (Compaction)
  触发条件: tokens > 120K
  操作: 中间过程 → 摘要

L2 策略: 子代理隔离 (Subagent Offload)  
  触发条件: tokens > 140K
  操作: 把大段独立工作委托给子代理（见 2.7）

L3 策略: 上下文重置 (Context Reset)
  触发条件: tokens > 160K
  操作: 保留 system prompt + 当前任务 + 进度文件 + 最后 2 轮
        其余全部清除，从进度文件恢复上下文
```

---

### 2.5 任务拆解 (Task Decomposition) —— 解决 Context 溢出的根源

**一句话总结**：把大任务拆解成 JSON 格式的子任务清单，强制 Agent 每次迭代只处理一件事，完成后标记状态再取下一件。从根源上防止单个 context 承担太多。

#### 2.5.1 为什么要在源头解决问题？

上下文工程（2.4）解决的是"context 太多了怎么办"——这是**症状处理**。
任务拆解解决的是"为什么 context 会这么多"——这是**根源治理**。

```
一个 Agent 如果一次试图做 10 件事：
  → 思考 10 件事的策略 → 执行 10 件事的操作 → 看 10 件事的结果
  → 每一轮 messages 都塞满与 10 件事相关的信息
  → Context 必然爆炸

同一个 Agent 如果一次只做 1 件事：
  → 思考 1 件事的策略 → 执行 1 件事的操作 → 看 1 件事的结果 → 标记完成
  → 下一件事开始时，前一件事的细节可以被压缩
  → Context 保持精简
```

#### 2.5.2 任务拆解清单结构

```json
{
  "task_id": "refactor-user-module",
  "created_at": "2025-05-21T10:00:00Z",
  "items": [
    {
      "id": "1",
      "phase": "analyze",
      "description": "分析 UserService 的当前结构：找出所有公共方法、依赖、被引用位置",
      "status": "completed",
      "output_summary": "UserService 有 12 个公共方法，被 8 个文件引用，核心依赖是 UserRepository 和 EmailService",
      "completed_at": "2025-05-21T10:05:00Z"
    },
    {
      "id": "2",
      "phase": "plan",
      "description": "制定重构计划：确定哪些方法需要重命名、抽取、合并",
      "status": "completed",
      "output_summary": "计划重命名 3 个方法、抽取 1 个接口、合并 2 个重复方法",
      "completed_at": "2025-05-21T10:08:00Z"
    },
    {
      "id": "3",
      "phase": "act",
      "description": "抽取 IUserService 接口",
      "status": "completed",
      "verification": "编译通过，接口包含所有公共方法签名",
      "completed_at": "2025-05-21T10:15:00Z"
    },
    {
      "id": "4",
      "phase": "act",
      "description": "重命名 getUserName → getUsername",
      "status": "in_progress"
    },
    {
      "id": "5",
      "phase": "act",
      "description": "重命名 getUserById → findById",
      "status": "pending"
    },
    {
      "id": "6",
      "phase": "act",
      "description": "合并 createUser 和 insertUser 为 createUser",
      "status": "pending"
    },
    {
      "id": "7",
      "phase": "verify",
      "description": "运行全量单元测试，确保重构不破坏功能",
      "status": "pending"
    },
    {
      "id": "8",
      "phase": "verify",
      "description": "检查是否有遗漏的旧方法名引用",
      "status": "pending"
    }
  ]
}
```

#### 2.5.3 拆解原则

| 原则 | 说明 | 示例 |
|------|------|------|
| **原子性** | 每个子任务只做一件事 | "重命名 getUserName" 而非 "重命名所有用户方法" |
| **可验证性** | 每个子任务有明确的验证标准 | "编译通过 + grep 确认旧名 0 引用" |
| **有产出** | 每个子任务完成后产生可检查的产出 | 文件 diff、测试结果、截图 |
| **有序依赖** | 后续子任务依赖前序子任务的产出 | 分析 → 计划 → 执行 → 验证 |
| **可并行标记** | 标注哪些子任务可以并行 | `"parallel_group": "rename-methods"` |

#### 2.5.4 Agent 如何与任务清单交互

```python
# Agent 每轮迭代只关注当前子任务：
current_subtask = get_next_pending_subtask(task_list)

# Phase 1: GATHER —— 只收集与当前子任务相关的上下文
context = gather_for_subtask(current_subtask)  # 不是收集全局上下文！

# Phase 2: ACT —— 只执行当前子任务
result = execute_subtask(current_subtask)

# Phase 3: VERIFY —— 只验证当前子任务的产出
verification = verify_subtask(current_subtask, result)

# Phase 4: ITERATE
if verification.passed:
    current_subtask.mark_completed()
    current_subtask.output_summary = summarize(result)  # 产出的摘要，不是全部细节
    save_progress()  # 持久化
```

---

### 2.6 验证循环 (Verify Loop) —— 解决 Tool 错误吞并（任务层面）

**一句话总结**：工具编排（2.2）解决的是**工程层面**的结构化错误（异常被吞），验证循环解决的是**任务层面**的真机验证（功能真的对吗）。两者是搭档关系。

#### 2.6.1 双重错误，双重防线

```
工程层面: Tool 调用失败了 Agent 要能看见
  → 靠工具编排 (Tool Orchestration) 解决
  → 确保 tool_result 总是结构化的，包含 success/error/suggestion

任务层面: Tool 调用成功了但结果不对 Agent 要能察觉
  → 靠验证循环 (Verify Loop) 解决
  → 用真机工具去实际验证功能，不信任 LLM 的自我判断
```

#### 2.6.2 验证循环的工作流

```python
def verify_loop(subtask, result):
    """
    对每个完成的子任务进行真机验证
    验证失败 → 标记失败原因 → Agent 重新执行 → 再次验证
    最多重试 3 次
    """
    for attempt in range(3):
        verification = run_verification(subtask, result)
        
        if verification.passed:
            subtask.mark_completed()
            subtask.verification_output = verification.output
            return True
        
        # 验证失败 → 把失败信息反馈给 Agent
        failure_context = {
            "attempt": attempt + 1,
            "test_output": verification.output,
            "expected": verification.expected,
            "actual": verification.actual,
            "suggestion": f"第 {attempt + 1} 次尝试失败。{verification.hint}"
        }
        
        if attempt < 2:  # 还有重试机会
            result = agent.retry_subtask(subtask, failure_context)
        else:
            subtask.mark_failed(failure_context)
            return False
```

#### 2.6.3 验证工具矩阵

| 验证对象 | 验证工具 | 验证方式 |
|---------|--------|---------|
| **代码正确性** | pytest, unittest, Jest | 运行单元测试、集成测试 |
| **代码风格** | ruff, eslint, prettier | 运行 linter |
| **类型安全** | mypy, pyright, tsc | 运行类型检查 |
| **UI 正确性** | Playwright, Selenium | 截屏对比、元素断言 |
| **API 正确性** | curl, httpx, Postman | 发送 HTTP 请求，断言响应 |
| **数据库变更** | 测试数据库迁移 | 在临时数据库上跑 migration |
| **文件生成** | diff, wc, file 命令 | 检查文件存在、大小、格式 |
| **构建产物** | npm build, go build | 编译通过 + 产物大小检查 |

#### 2.6.4 验证结果格式

```python
class VerificationResult:
    passed: bool
    tool: str                    # 用哪个工具验证的
    output: str                  # 工具的标准输出（截断版）
    expected: str                # 预期行为
    actual: str                  # 实际行为
    hint: str                    # 失败时给 Agent 的修复提示
    artifacts: list[str]         # 验证过程中产生的截图、日志路径
    
    # 示例：
    # {
    #   "passed": false,
    #   "tool": "pytest tests/test_user_service.py::test_get_username",
    #   "output": "FAILED: AssertionError: Expected 'getUsername' but got 'getUserName'",
    #   "expected": "方法 getUsername 存在且可调用",
    #   "actual": "调用 user.getUsername() 抛出 AttributeError",
    #   "hint": "方法可能还未重命名，请确认 UserService.java 中确实将 getUserName 改为 getUsername",
    #   "artifacts": []
    # }
```

#### 2.6.5 为什么不能用 LLM 自评代替？

这是一个常见的错误想法："让 LLM 自己检查自己的工作不就行了？"

```
LLM 自评的问题：
  1. LLM 不知道自己的输出是不是幻觉
     - "我已经修改了所有引用" → 实际还有 3 个文件没改
     - LLM 在说这句话时，它真的"认为"自己改了 —— 但它没有去验证
  
  2. LLM 倾向于同意自己
     - 如果你问 LLM "你做对了吗？"，它大概率说"做对了"
     - Self-consistency 在同一个模型上不成立
  
  3. LLM 无法感知外部状态
     - 只有真机运行才能知道代码能不能编译
     - 只有真机运行才能知道测试能不能通过
     - 只有 Playwright 截屏才能看到 UI 到底渲染成了什么样
```

**黄金法则**：LLM 的"我觉得做好了"是假信号。真信号只有来自**外部验证工具**。

---

### 2.7 子代理分治 (Subagent Delegation) —— 解决 Context 溢出的另一维度

**一句话总结**：把特定子任务委托给拥有独立 context 的子 Agent，子 Agent 在自己的 messages 列表中工作，完成后只把**最终结果**返回给主 Agent。主 Agent 的 context 完全不受子任务中间过程的污染。

#### 2.7.1 对比：不用子代理 vs 用子代理

```
不用子代理（一切在主 context 中）:
  messages = [
    system_prompt,           # 2K tokens
    用户任务,                 # 1K
    主 Agent 分析 (10 轮),     # 30K
    子任务 A 执行 (15 轮),     # 45K  ← 中间过程全部在主 context 里
    子任务 B 执行 (20 轮),     # 60K  ← 中间过程全部在主 context 里
    子任务 C 执行 (12 轮),     # 36K  ← 中间过程全部在主 context 里
  ] 总计: ~174K tokens → 接近爆炸

用子代理（子任务在独立 context 中）:
  主 Agent messages = [
    system_prompt,           # 2K
    用户任务,                 # 1K
    主 Agent 分析 (10 轮),     # 30K
    子任务 A 结果 (1 条消息),   # 0.5K  ← 只有最终结果！
    子任务 B 结果 (1 条消息),   # 0.5K  ← 只有最终结果！
    子任务 C 结果 (1 条消息),   # 0.5K  ← 只有最终结果！
  ] 总计: ~34.5K tokens → 绰绰有余

节省了 139.5K tokens（80%）！
```

#### 2.7.2 子代理的设计

```python
class Subagent:
    """
    子代理是一个拥有独立上下文的、专注于单一任务的小型 Agent
    """
    def __init__(self, name, system_prompt, tools, max_iterations=15):
        self.name = name
        self.messages = [
            {"role": "system", "content": system_prompt}
        ]
        self.tools = tools           # 子代理可用工具（通常是主 Agent 的子集）
        self.max_iterations = max_iterations
    
    def execute(self, task_description: str) -> SubagentResult:
        """
        在自己的隔离 context 中执行任务
        主 Agent 的 messages 不受任何影响
        """
        self.messages.append({"role": "user", "content": task_description})
        
        for i in range(self.max_iterations):
            response = call_llm(self.messages, self.tools)
            # ... 正常的四相循环逻辑 ...
        
        # 只返回最终结果，不返回中间过程
        return SubagentResult(
            success=True,
            summary="子任务的最终产出摘要",
            artifacts=["path/to/output1.txt", "path/to/output2.png"],
            metrics={"iterations": i, "tokens_consumed": total_tokens}
        )
```

#### 2.7.3 主 Agent 如何调度子代理

```python
# 主 Agent 的视角：
# 子代理调用就像调用一个高级的"函数" —— 
# 给它任务描述，拿到结果摘要，完全不用关心中间过程

messages = [
    {"role": "system", "content": main_system_prompt},
    {"role": "user", "content": "分析整个项目的测试覆盖率并生成报告"}
]

# 主 Agent 拆解后发现：
#   子任务 1: 运行 pytest --cov → 委托给 "test-runner" 子代理
#   子任务 2: 解析覆盖率 XML → 委托给 "coverage-parser" 子代理
#   子任务 3: 生成 HTML 报告 → 委托给 "report-generator" 子代理

# 主 Agent 的 context 中只有：
messages.append({
    "role": "assistant", 
    "content": "[委托子代理 test-runner 执行 pytest --cov]"
})
messages.append({
    "role": "tool",  # 子代理返回的结果
    "content": json.dumps({
        "subagent": "test-runner",
        "result": "测试全部通过，覆盖率 87%，XML 已生成至 coverage.xml"
    })
})
# ↑ 就这么简短！15 轮子代理内部对话全部不可见
```

#### 2.7.4 子代理的三种模式

| 模式 | 说明 | 适用场景 |
|------|------|---------|
| **Fire-and-Forget** | 委托后不等结果，继续处理其他事 | 独立任务，如"跑测试" |
| **Request-Response** | 委托后等待结果，基于结果继续 | 有依赖关系，如"分析后根据结果决定下一步" |
| **Map-Reduce** | 多个子代理并行执行同类任务，结果汇总 | 批量操作，如"分析 10 个模块各自的覆盖率" |

#### 2.7.5 子代理的设计原则

1. **职责单一**：一个子代理只做一件事。Analyzer 只分析、Generator 只生成、Tester 只测试
2. **工具最小化**：只给子代理完成任务所需的最小工具集。不给删除文件的权限如果它只需要读取
3. **输入明确**：主 Agent 给子代理的任务描述必须精确、具体、包含所有必要信息
4. **输出结构化**：子代理返回的结果必须是结构化数据（JSON），不能是自由文本
5. **独立可验证**：每个子代理的产出必须可以独立验证，不依赖主 Agent 的上下文

---

### 2.8 GAN 启发三角色架构 —— 解决缺自动化评审

**一句话总结**：不是给单 Agent 打补丁，而是直接引入多 Agent 架构。借鉴 GAN（生成对抗网络）的博弈思想，把一个任务的执行流拆成三个独立角色：Planner（规划者）、Generator（生成者）、Evaluator（评价者）。

#### 2.8.1 GAN 的核心思想

**什么是 GAN（生成对抗网络）**：

GAN 由两个神经网络组成：
- **生成器 (Generator)**：接受随机噪声，生成"伪造"数据（如图片）
- **判别器 (Discriminator)**：接受数据，判断是"真实"还是"伪造"

训练过程是两者的博弈：
1. 生成器试图骗过判别器
2. 判别器试图识别伪造
3. 两者在对抗中不断提高
4. 最终生成器产生的数据足以"以假乱真"

**我们借鉴的不是 GAN 的训练过程，而是它的博弈理念**：
- 两个独立的评判视角比一个自我评判视角好得多
- 博弈（Adversarial）比自我审查（Self-review）有效得多
- 分离"做"和"评"的职责

#### 2.8.2 三角色架构

```
                     ┌──────────────┐
                     │   PLANNER    │
                     │   规划者      │
                     │              │
                     │ "该做什么？   │
                     │  怎么做？"    │
                     └──────┬───────┘
                            │ 任务计划 (JSON)
                            ▼
                     ┌──────────────┐
                     │  GENERATOR   │
                     │   生成者      │
                     │              │
                     │ "我来做。"    │
                     └──────┬───────┘
                            │ 产出 (Code / Doc / Report)
                            ▼
                     ┌──────────────┐
                     │  EVALUATOR   │
                     │   评价者      │
                     │              │
                     │ "做对了吗？   │
                     │  打几分？"    │
                     └──────┬───────┘
                            │
                    ┌───────┴───────┐
                    ▼               ▼
               ✅ 通过          ❌ 不通过
            (交付用户)      (返回 Generator 修改)
```

#### 2.8.3 三角色的职责与约束

**Planner（规划者）**：
```
职责：
  - 分析用户需求
  - 拆解任务为可执行的步骤
  - 为每个步骤定义验收标准
  - 对 Generator 完全透明

约束：
  - 不写代码，只写计划
  - 必须输出结构化 JSON
  - 必须为每个步骤定义 verify 方式
```

**Generator（生成者）**：
```
职责：
  - 按 Planner 的计划执行
  - 产出实际的代码/文档/产物
  - 对自己产出的质量负责
  - 修补 Evaluator 反馈的问题

约束：
  - 不能修改计划（只能请求 Planner 修改）
  - 每步产出后必须附带自我检查清单
  - 不能查看 Evaluator 的评分标准（防止作弊）
```

**Evaluator（评价者）**：
```
职责：
  - 独立评价 Generator 的产出
  - 不看 Generator 的中间过程，只看最终结果
  - 对照 Planner 的验收标准逐项打分
  - 给出具体的、可操作的修复建议

约束：
  - 不能与 Generator 共享 context
  - 不能直接修改代码（只能评价）
  - 评价必须有具体的证据支撑（测试输出、截屏、diff）
  - 低于 7/10 分 → 必须退回修改
```

#### 2.8.4 评价标准体系

```json
{
  "evaluation_id": "eval-2025-05-21-001",
  "task": "重构 UserService 模块",
  "generator_output": "commit a1b2c3d",
  "scoring": {
    "correctness": {
      "score": 8,
      "max": 10,
      "evidence": "31/31 测试通过",
      "issues": []
    },
    "completeness": {
      "score": 7,
      "max": 10,
      "evidence": "grep 检查发现 2 处遗漏的旧方法名引用",
      "issues": [
        {"file": "EmailService.java:45", "problem": "仍在使用旧方法名 getUserName"}
      ]
    },
    "code_quality": {
      "score": 8,
      "max": 10,
      "evidence": "ruff 检查通过，0 个新警告",
      "issues": []
    },
    "consistency": {
      "score": 9,
      "max": 10,
      "evidence": "重命名模式一致：全部采用 camelCase",
      "issues": []
    }
  },
  "total_score": 8.0,
  "verdict": "PASS_WITH_MINOR_FIXES",
  "required_fixes": [
    "EmailService.java:45: getUserName → getUsername",
    "ReportGenerator.java:102: getUserName → getUsername"
  ]
}
```

#### 2.8.5 与验证循环的关系

```
验证循环 (Verify Loop):       GAN 三角色:
  ↓                             ↓
  每个子任务完成后              整个任务完成后
  用真机工具自动化验证           用独立 Agent 综合评审
  (pytest / Playwright / linter)  (多维度打分)
  ↓                             ↓
  快速、频繁、自动化            深度、综合、博弈性
  ↓                             ↓
  防小错                        防大错
```

这两者是互补的：验证循环在每个微观步骤把关，GAN 评审在宏观层面把关。

#### 2.8.6 三角色架构的优势与代价

| 优势 | 代价 |
|------|------|
| 评价独立，不受 Generator 自我欺骗 | API 调用量是单 Agent 的 3+ 倍 |
| 评分有证据支撑，不是"感觉" | 三个 Agent 之间的协调增加延迟 |
| 多次退回修改提升最终质量 | 需要设计三个不同的 system prompt |
| 可扩展——Evaluator 可以升级模型而不影响执行 | Evaluator 的评价质量取决于它自身的能力 |

---

## 三、三支柱正交视角

八大机制是从**工程实现**视角看 Harness。现在换一个视角，
从**产品/系统设计**的角度，用三个正交支柱来俯瞰整个 Harness 系统。

### 3.1 输入流 (Input Flow) —— Agent 能看见什么

**核心问题**：Agent 的"视野"里有什么信息？这些信息的质量、时效性、完整性如何？

```
输入流的层次：
┌─────────────────────────────────────┐
│  固定层 (Static)                     │
│  ├── System Prompt (角色定义、行为约束) │
│  ├── CLAUDE.md (项目规范、编码约定)     │
│  └── Tool Definitions (可用工具清单)   │
├─────────────────────────────────────┤
│  会话层 (Session)                    │
│  ├── 用户任务与历史消息               │
│  ├── Session Compaction (压缩后摘要)  │
│  └── 多轮对话上下文                  │
├─────────────────────────────────────┤
│  动态层 (Dynamic)                    │
│  ├── Progress File (当前进度)        │
│  ├── 子代理返回结果                  │
│  ├── 检索结果 (RAG)                  │
│  └── 环境信息 (CWD, Git Status, 时间)│
└─────────────────────────────────────┘
```

**输入流代表机制**：
- 上下文工程（决定什么进入 context）
- 任务拆解（限制当前视野的范围）
- 子代理分治（隔离子任务的视野）
- 进度跟踪（提供跨 session 的视野连续性）

### 3.2 控制流 (Control Flow) —— Agent 能干什么

**核心问题**：Agent 行为的边界在哪里？什么能做、什么不能做、什么需要审批才能做？

```
控制流的层次：
┌─────────────────────────────────────┐
│  允许层 (Allowed)                    │
│  ├── 读取文件                        │
│  ├── 搜索代码 (grep/glob)            │
│  ├── 执行沙箱化命令                   │
│  └── 调用子代理                      │
├─────────────────────────────────────┤
│  审批层 (Approval Required)           │
│  ├── 修改文件 (Edit)                  │
│  ├── 执行 shell 命令 (Bash)           │
│  ├── 网络请求 (WebFetch)              │
│  └── Git 操作 (commit/push)           │
├─────────────────────────────────────┤
│  禁止层 (Forbidden)                   │
│  ├── rm -rf / (破坏性操作)            │
│  ├── 读取 .env / 密钥文件             │
│  ├── force push 到 main               │
│  └── 修改系统配置                     │
└─────────────────────────────────────┘
```

**控制流代表机制**：
- 四相循环（控制执行节奏）
- 工具编排（结构化工具调用+权限前置检查）
- 权限闸 + Hook 系统（27 种 Hook 定义行为边界）
- GAN 三角色（三方博弈，相互制约）

### 3.3 状态流 (State Flow) —— Agent 能跑多久

**核心问题**：Agent 如何在长时间运行中保持稳定？面对 context 膨胀、token 消耗、进程中断等自然熵增，系统如何对抗？

```
状态流的生命周期：
┌──────────────────────────────────────┐
│  会话开始                             │
│  ├── 读取 progress.json (恢复上下文)   │
│  ├── 恢复 git 工作区状态               │
│  └── 检查 token 预算                  │
├──────────────────────────────────────┤
│  运行中                               │
│  ├── Token Budget 监控               │
│  │   ├── 正常 (< 80K): 正常运行       │
│  │   ├── 警告 (80-120K): 精简输出    │
│  │   ├── 压缩 (120-160K): Compaction │
│  │   └── 重置 (> 160K): 创建新 Session│
│  ├── 进度持久化 (每子任务完成)         │
│  └── checkpoint (git commit)         │
├──────────────────────────────────────┤
│  会话结束                             │
│  ├── 正常结束: 保存最终状态            │
│  ├── 异常终止: progress.json 保留断点  │
│  └── 超时终止: Token 预算耗尽 → 优雅降级│
└──────────────────────────────────────┘
```

**状态流代表机制**：
- 上下文工程（Token 监控、压缩、重置）
- 进度跟踪（断点续传）
- Token 预算（硬上限 + 优雅降级）

---

## 四、两张正交对照矩阵

### 4.1 矩阵一：八大失效 × 八大机制

这是**问题 → 解决**的映射矩阵，告诉你哪个机制主要解决了哪个失效，以及是否存在交叉覆盖。

```
              四相   工具   进度   上下文  任务   验证   子代理  GAN
              循环   编排   跟踪   工程    拆解   循环   分治   三角色
              ────  ────  ────  ──────  ────  ────  ────  ──────
① 循环失控    ★★★    ·     ·      ·      ·     ·      ·      ·
② Context溢出  ·     ·     ·     ★★★    ★★★   ·  ★★★    ··
③ Cache Miss   ·     ·     ·     ★★★    ·     ·      ·      ·
④ Tool错误吞   ·    ★★★    ·      ·      ·    ★★★    ·      ·
⑤ 状态丢失     ·     ·    ★★★    ·      ·     ·      ·      ·
⑥ 缺权限闸     ·    ★★★    ·      ·      ·    ·      ·     ★★★
⑦ 缺自动评审   ·     ·     ·      ·      ·    ★★★    ·     ★★★
⑧ 成本失控     ·     ·     ·     ★★★    ·     ·      ·      ·

★★★ = 主要解决
★★  = 重要辅助
·   = 间接影响
```

**解读这张矩阵**：
1. **Context 溢出的三道防线**：上下文工程（压缩）、任务拆解（源头控制）、子代理分治（隔离）形成三道独立防线
2. **Tool 错误的两层防线**：工具编排（工程层）+ 验证循环（任务层），一横一纵
3. **自动评审的双重保障**：验证循环（微观自动检查）+ GAN 三角色（宏观博弈评审）

### 4.2 矩阵二：八大机制 × 三支柱

这是**机制 → 支柱**的映射矩阵，告诉你在设计 Harness 时，每个机制属于哪个支柱的范畴。

```
              输入流   控制流   状态流
              ──────  ──────  ──────
四相循环          ·      ★★★     ★★
  (控制迭代节奏)          节奏控制  防止无限运行

工具编排          ★      ★★★      ·
  (调用+返回)    返回内容  调用权限

进度跟踪         ★★★      ·      ★★★
  (断点续传)    恢复上下文          持久化+恢复

上下文工程       ★★★      ·      ★★★
  (窗口管理)    看什么            压缩+重置

任务拆解         ★★★     ★★       ★
  (化整为零)    聚焦视野  分步约束  减少单次负担

验证循环          ★      ★★★      ★
  (真机验证)    验证结果  判定流程  失败分支

子代理分治       ★★★     ★★      ★★★
  (隔离执行)    隔离视野  分治管控  独立生命周期

GAN三角色         ★      ★★★      ★★
  (三方博弈)    评审输入  多方制衡  退回→重试
```

**解读这张矩阵**：
1. **输入流重型机制**：上下文工程、任务拆解、子代理分治（都在控制"Agent 看见什么"）
2. **控制流重型机制**：四相循环、工具编排、验证循环、GAN 三角色（都在控制"Agent 怎么行动"）
3. **状态流重型机制**：上下文工程、进度跟踪（都在控制"Agent 能稳定运行多久"）
4. 这三大支柱是正交的——你可以在不改动输入流配置的情况下升级控制流逻辑，反之亦然

---

## 五、本章小结

1. **八大核心机制**构成 Harness Engineering 的完整武器库，每一个都对应解决 Naive Agent 的一种或多种失效形态。

2. **八大机制的协作关系**：
   - 四相循环是骨架（执行节律）
   - 工具编排 + 验证循环是肌肉（做对的事 + 检验做对了）
   - 上下文工程 + 任务拆解 + 子代理分治是内存管理（context 的空间和时间分配）
   - 进度跟踪是保险（crash 后可恢复）
   - GAN 三角色是质检（交付前最后一道关）

3. **三支柱正交视角**提供了另一个维度——不只是"怎么实现"，更是"设计什么"：
   - 输入流：Agent 能看见什么
   - 控制流：Agent 能干什么
   - 状态流：Agent 能跑多久

4. **两张对照矩阵**让你在任何时候都能快速定位：面对一个具体问题，应该启动哪个机制来解决；在设计 Harness 时，每个机制在你的系统架构中属于哪个支柱。

> **下一章 [第三幕 · 实](./03-实-工程落地与产品.md)**，我们将看到这些机制在真实产品中是如何落地的，从 Claude Code 到 DeepAgents，从 LangChain 到 Cursor，以及对 Harness Engineer 能力模型的完整定义。
